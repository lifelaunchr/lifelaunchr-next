@AGENTS.md

## ⚠️ CRITICAL — TWO REPO RULE (READ FIRST, EVERY SESSION)

**This repo (`lifelaunchr-next`) is FRONTEND ONLY (Next.js → Vercel).**
**ALL backend/API/AI/DB changes go in `lifelaunchr-app-3` (FastAPI → Render).**

| What | Repo |
|------|------|
| UI pages, components, styles | `lifelaunchr-next` ← YOU ARE HERE |
| API routes, chat engine, DB, AI logic | `lifelaunchr-app-3` |

---

# LifeLaunchr / Soar — Deployment Reference

> Last updated: 2026-07-09 (v1.5.28).

## Version History

| Version | Date | Notes |
|---------|------|-------|
| v1.5.28 | 2026-07-09 | Version-check banner — reload long-open tabs onto fresh code after a deploy (next#85, frontend-only). **Why:** a long-open tab (a counselor who leaves `/dashboard` open for hours) keeps running the *old* bundle after a deploy until it manually reloads — exactly the shape of **next#79** (Ranna's stale-bundle blank: her account renders perfectly on current code, confirmed via Clerk impersonation across all surfaces/widths/reloads with her extension active and no `#418` — so her blank was old code her tab never refreshed off of). Our HTML is `no-store` and JS chunks are content-hashed, so any *real* reload already lands on fresh code; the only stragglers are tabs that never re-request. This closes that gap. **How (3 pieces):** (1) **`GET /api/version`** (`src/app/api/version/route.ts`, `export const dynamic = 'force-dynamic'`, `Cache-Control: no-store`) returns the *currently-deployed* SHA (`process.env.VERCEL_GIT_COMMIT_SHA`) at request time — always reflects what prod is serving *now*. Added to `isPublicRoute` in **`src/proxy.ts`** (no auth, no PII). (2) The root layout **`src/app/layout.tsx`** (a server component) reads `process.env.VERCEL_GIT_COMMIT_SHA` at request time and passes it as a `buildId` prop to `<VersionBanner>` — the SHA of the deploy that server-rendered the page (i.e. the build the tab loaded). (3) **`src/components/VersionBanner.tsx`** (`'use client'`) polls `/api/version` every 5 min **+ on `visibilitychange`** (tab refocus — the fast trigger); when the server SHA differs from its `buildId` prop (and isn't `'dev'`, and wasn't already dismissed for that version via a `useRef`), shows a dismissible top banner (navy, `#7dd3fc` pill) with a one-click **Reload** (`window.location.reload()`). **Never force-reloads** — a counselor may be mid-typing a report. Renders `null` until a mismatch is found *post-mount*, so server + first client render both produce nothing → **can't cause a hydration mismatch** (which is the next#79 failure mode itself — deliberately mirrors the safe `BetaBanner`/`useState(false)` pattern). Swallows fetch failures (offline never nags). Mounted in the layout directly above `<BetaBanner />`. **Design note:** the build SHA is delivered via a runtime server-rendered prop, *not* a build-time-inlined client env var — an initial `next.config.ts` `env: { NEXT_PUBLIC_BUILD_ID }` approach resolved to `'dev'` in the client bundle under Turbopack (client inlining didn't take), silently disabling the check; the runtime read (the same mechanism `/api/version` uses successfully) is reliable. **Nuance (important):** this can't rescue a tab *already* stuck on stale code — that old bundle doesn't contain the checker. It protects *going forward*, once a tab has loaded a build that includes it; the current stale tab still needs one manual reload (which `no-store` HTML makes trivial). **Depends on Vercel Skew Protection being OFF** (the default) — with it on, an old tab's `/api/version` call is pinned to its original deployment and never sees the new SHA, so the banner never fires. Tested on staging via two sequential deploys (open tab on deploy A → push deploy B → tab-refocus → banner appears → Reload lands on B). No backend changes. No new env vars (uses Vercel's built-in `VERCEL_GIT_COMMIT_SHA`). `tsc --noEmit` + eslint + `next build` clean; `/api/version` registers as a dynamic (ƒ) route. |
| v1.5.27 | 2026-07-08 | Essay Plan — crash-proof the student-facing summaries against any LLM output shape (next#84, frontend-only). **Trigger:** a counselor opening a student's Essay Plan → **UC PIQs** tab hit "Something went wrong" (the v1.5.24 `dashboard/error.tsx`-style boundary caught it); console showed **Minified React error #31** ("Objects are not valid as a React child"). **Root cause:** the Essay Plan is Claude-generated JSON, and the `student_facing_summary` field is only *soft*-instructed in the backend prompt — so its shape varies **per student and per module**: usually a string, sometimes a `string[]`, and for some students a per-PIQ **object** like `{ piq_3: "…", piq_4: "…" }`. That's why it worked for most students (string/array) and crashed for others (object). The old `summaryText()` helper in `EssayPlanPanel` (`src/components/writing/WritingCoachView.tsx`) only handled `string | string[]` and returned an object **unchanged** → object rendered as a React child → #31. Compounding it, the **CommonApp** and **Why Major** tabs rendered their summary *directly* (`{ca.student_facing_summary}` / `{wm.student_facing_summary}`), bypassing `summaryText` entirely — so they'd crash the same way if the model ever returned an object for them. **Fix (two moves, one file):** (1) hardened `summaryText` to `(s: unknown) => string` — stringifies **any** shape: `null`/`undefined`→`''`, string→itself, array→recursively joined, **object→`Object.entries` mapped** (keys matching `/^piq[_\s-]?(\d+)$/i` formatted as `"PIQ N: …"`, others just their value), everything joined with blank lines; (2) routed **every** summary render through it — CommonApp `<p>` and Why Major `<p>` + `CopyButton` now call `summaryText(...)` (UC PIQs and the CommonApp CopyButton already did). Now all three summary-bearing tabs (CommonApp, UC PIQs, Why Major) are crash-proof regardless of what the model returns; Top 10 has no summary field. **Verified against the real crashing data:** ran the exact hardened logic over the production student's actual object — it produces a clean `"PIQ 3: … / PIQ 4: …"` string, no throw; array/string/undefined/null/empty-object all return strings, so no object ever reaches React. `tsc --noEmit` + eslint + `next build` (Turbopack) clean. No interface/type churn needed (all renders go through `summaryText(unknown)`). No backend change (the underlying `student_facing_summary` shape is unchanged; the frontend is now tolerant of all of them). No new deps/env/API. |
| v1.5.26 | 2026-07-07 | Essays page — Common App essay now shows + required-first layout (app#165, backend + frontend; ships together). **Part A (backend, see app CLAUDE.md v1.5.26):** `/essays/prompts` now fetches shared platform essays (Common App personal statement, UC PIQs, Coalition) by the student's application platforms instead of the arbitrary anchor school, so they appear for the right students; Coalition is suppressed when a school also accepts Common App. **Part B (frontend, `src/app/essays/page.tsx`):** required-first redesign of the essay list. New reusable **`CollapsibleSection`** component (collapsed by default, renders `null` when empty; typed via a new `type ReactNode` import). The prompts render (previously a flat list of every essay) was rewritten as an IIFE that splits groups into required vs optional: (1) a **summary bar** — "N required essays to write across M schools" (`requiredCount` = platform required + per-school required supplements; `!group.optional`); (2) **platform essays** — required cards shown, optionals collapsed under "Optional platform essays (N)"; (3) **per school** — required supplements shown, optional supplements collapsed under "Optional (N)", program-specific collapsed under "Program-specific (N)" (always collapsed — situational). `EssayGroupCard` unchanged. No data/API change (the data comes from Part A). Validated on staging (NYU/UCLA/Pomona student: 9 required across 3 schools, Common App + UC PIQ shown, Coalition/optionals/program-specific correctly tucked away). `tsc --noEmit` + eslint + `next build` clean. |
| v1.5.25 | 2026-07-07 | Wow Writing Workshop attribution on the Common App module (next#80, frontend-only). Adds the partner-approved credit line — *"Some concepts in our Common App module are based on elements of the Wow Method, developed by Wow Writing Workshop, and are used with permission"* (linking wowwritingworkshop.com) — as a small italic footer (`text-[10px] text-slate-500 italic`, `border-t` above it, underlined `not-italic` link) in **two** Common App surfaces of `src/components/writing/WritingCoachView.tsx`, scoped to Common App only: (1) the **Essay Plan → CommonApp tab** — bottom of the `activeTab === 'commonapp' && ca` block in `EssayPlanPanel`; (2) the **CommonApp module exercise panel** — bottom of `AssignPanel`, gated `sectionKey === 'commonapp' && !loading && !fetchError && exercises.length > 0`. (A third placement in the student assignment-section loop was added then removed on review — the student list is a flat mixed-section view where a "Common App module" footer doesn't cleanly belong.) No backend changes. No new deps/env. `tsc --noEmit` + eslint clean. |
| v1.5.24 | 2026-07-07 | Sentry-on-Turbopack fix + hydration-crash hardening for the My Students dashboard (next#78 + next#79, frontend-only). **Trigger:** a counselor's `/dashboard` rendered a permanently blank student list. Diagnosed on a screen-share: backend returned all 26 students correctly; her console showed one error — `Minified React error #418` (hydration mismatch) with `MessagePort` (Clerk cross-tab token broadcast) in the stack. Ruled out (with evidence) data, student count, Grammarly/DOM-injecting extensions, and stale bundle/service worker — her error chunk `18c2rstveyg8s.js` is in *current* prod HTML and both her chunks 200, so she runs current code; the mismatch is environmental to her machine (an extension's timing + the Clerk broadcast landing mid-hydration), and with **no error boundary anywhere** a throw during render/hydration-recovery unmounts the whole tree to a silent blank. **next#78 — Sentry actually initializes now:** Turbopack does not load the legacy `sentry.client.config.ts` (Sentry's webpack plugin injected it; Turbopack skips that), so the browser SDK never initialized and every `captureException` (incl. next#77) was inert. Moved client init to `src/instrumentation-client.ts` (the file Next loads under Turbopack — must be in `src/` since the project uses a src dir), preserving the offline transport (next#77) and adding `onRouterTransitionStart`. Added `src/instrumentation.ts` with `register()` (lazy-imports `../sentry.server.config` / `../sentry.edge.config` by runtime) + `onRequestError`. Deleted the dead `sentry.client.config.ts`. **next#79 — error boundaries + mounted-gate:** (1) `src/app/global-error.tsx` — root boundary, reports to Sentry, friendly reload fallback (replaces raw blank when the root layout throws). (2) `src/app/dashboard/error.tsx` — segment boundary: "We couldn't load your students" message, **Reload**, and **Clear cached data & reload** (unregisters same-origin service workers + clears same-origin caches, then reloads); reports to Sentry. Converts the silent blank into an actionable, reported error — reaches her because she's on current code. (3) `src/app/dashboard/page.tsx` — mounted-gate: a `mounted` flag renders a deterministic pre-mount placeholder (nav + "My Students" + "Loading…") on server and first client render, then draws the interactive dashboard only after mount, so the roster is a client render (not hydrated) and a DOM-mutating extension can't tear it down. Defensive hardening. **Root cause later confirmed definitively:** React's own `#418` text (decoded via `react.dev/errors/418?args[]=HTML`) explicitly lists "a browser extension which messes with the HTML before React loaded" as a cause; reproduced locally — `next dev` (dev build) is CLEAN, `next build && next start` (production build, same machine + same `web-capture-extension-frames.js` extension) REPRODUCES the `#418` — so it is the production build's fast hydration hitting the timing race with the extension's DOM injection (dev's slower/staggered hydration misses it). Our own code audited clean (`layout.tsx`, `BetaBanner`, `CookieBanner` all use correct `useEffect`/`useState(false)` client-only patterns; no `Date.now`/`Math.random`/`typeof window` in render). Chat shows the `#418` but is recoverable (page works); only the dashboard's heavy render turned it fatal. Sentry now captures any real recurrence (verified: `__SENTRY__ = true`, session envelopes delivered from staging). `tsc --noEmit`, eslint, and `next build` (Turbopack) all clean. No new API calls. No new env vars. No backend changes. Issue next#79 widened from "chat page" to all Clerk-wrapped pages (dashboard highest-impact). |
| v1.5.23 | 2026-07-04 | Invited-user onboarding: skip the role picker via `is_invited`, not the localStorage token (next#76, frontend + backend). **Root cause:** invited users saw the role picker because the skip logic in `src/app/onboarding/page.tsx` was gated on `localStorage.getItem('migration_invite_token')`. Clerk auth is on `soar.lifelaunchr.com`, the app on `withsoar.ai` (satellite); the token is stored on the auth origin during sign-up, but onboarding lands on `withsoar.ai` — localStorage is per-origin, so the token reads `null` and the skip never runs. **Fix:** the effect drops the token gate (`if (!clerkUser) return`), always calls `/auth/sync` (its email match claims the invited row and returns `is_invited` + `account_type` + `is_tenant_admin` + `counselor_type`), redeems the token only if present on this origin, and routes by `account_type` only when `is_invited === true`. Routing: student → step 2 (profile); parent → step 4 (intro). **Counselor split by `is_tenant_admin`:** `false` (invited into an existing practice) → step 4 "You've joined [practice]" experience; `true` (requested access / owns their practice) → step 1 with the role pre-selected from `counselor_type` — IEC/coach get the practice field pre-filled with their tenant, school counselor gets state + school (NCES) fields — so they confirm/enter and save as tenant admin. Self-signups (`is_invited=false`) fall through to the picker unchanged. A brief "Linking your account…" screen shows on every onboarding load (the `/auth/sync` call) — fast, and prevents the picker flashing before invited users are routed. **Backend companion (app CLAUDE.md v1.5.23):** `/auth/sync` returns `is_invited`; approve flow captures `counselor_type` + fixes an `is_tenant_admin` over-grant; `/auth/sync` renames a lone placeholder tenant instead of orphaning it and demotes + cleans up on join; invite-colleague inherits the inviter's `counselor_type`. Validated on staging across every invite path, the two-counselor join, Add Family, counselor_type inheritance, and self-signup. No new API calls beyond `/auth/sync`. No new env vars. |
| v1.5.22 | 2026-07-02 | Chat connection-drop handling + Sentry offline transport (next#77) + two prior unshipped fixes. **`src/components/chat/ChatInterface.tsx` — stream-drop UX (next#77):** the `sendMessage` catch previously overwrote the assistant bubble with a generic "Sorry, something went wrong" for any non-abort stream error — wiping streamed content and implying data loss even though the backend worker finishes and persists the answer independently of the client connection (real incident: parent Kavita Patel saw "there was a problem," believed her answer was lost; it was saved server-side the whole time). Fix: (1) a `let receivedContent = false` flag set `true` on the first `text` SSE event; (2) the catch now branches — if `receivedContent`, keep any partial content and append "_The connection was interrupted. Your answer may have finished and been saved — reopen this session to check._"; if nothing streamed (offline/failed send — the common case, e.g. a new question sent while the connection is down), show "Couldn't reach Soar — please check your connection and try again." (nothing saved, safe to retry). (3) `import * as Sentry from '@sentry/nextjs'` + `Sentry.captureException(err, { tags: { area: 'chat_stream' } })` in the catch so stream drops are no longer invisible. The `finally` still resets `isStreaming` (input re-enables) — unchanged. **`sentry.client.config.ts` — offline transport (next#77):** wrapped the default transport in `Sentry.makeBrowserOfflineTransport(Sentry.makeFetchTransport)` so events captured while offline queue in IndexedDB and replay on reconnect (connection-drop errors happen with no network, so the default transport would otherwise silently drop them). **⚠️ Known limitation (next#78, filed):** frontend Sentry does not currently deliver ANY events — the client SDK never initializes because the app is on Next.js 16 / Turbopack, which does not load `sentry.client.config.ts` (needs `instrumentation-client.ts`; there is also no `instrumentation.ts`, so server/edge Sentry is uninitialized too). So the `captureException` + offline transport are correct but **inert** until next#78 migrates the config. The user-facing error messages above work regardless of Sentry. **`next.config.ts` + `/accept-invite` (`96b7fcb`, `e0d56c2`, prior unshipped from Jul 1):** removed a stale CDN-redirect comment and preserve the `?token=` query param when redirecting `/accept-invite` from `withsoar.ai` → `soar.lifelaunchr.com` (these were sitting untagged on `main`; bundled into this release). **Also filed:** next#79 (React #418 hydration crash on the chat page via Clerk MessagePort broadcasting a token refresh during hydration — pre-existing, not from this work). No new API calls. No new env vars. Backend companion: app#164 (reassurance heartbeat). |
| v1.5.21 | 2026-06-30 | Fix chat input lag in long sessions — memoize ChatMessage (next#72, frontend-only). **`src/components/chat/ChatMessage.tsx`:** Wrapped `ChatMessage` in `React.memo`. Three-line change: added `import { memo } from 'react'`; changed `export function ChatMessage(...)` to `export const ChatMessage = memo(function ChatMessage(...)`; closed with `})` instead of `}`. **Root cause:** `input` state (the textarea value) lived in `ChatInterface` alongside the `messages` array. Every keystroke called `setInput` → re-rendered `ChatInterface` → re-rendered every `ChatMessage`, including large HTML comparison documents (14–26 KB per message). With 60+ messages in a long session, the cost compounded on every keypress. **Fix:** `React.memo` performs a shallow prop comparison before re-rendering. On keystrokes, `message`, `onAddToList`/`onAddToScholarshipList`/`onAddToEnrichmentList` (all stable `useCallback` refs), and `addingToList`/`addingToScholarshipList`/`addingToEnrichmentList` (unchanged during typing) all compare equal — so all existing messages are skipped entirely. Only the textarea re-renders. `isStreaming` is `isStreaming && msg.id === streamingMessageId` — changes correctly when streaming starts/stops so the streaming message still updates. Validated: streaming, add-to-list buttons, and student switching all work correctly. No new components. No new API calls. No new env vars. No backend changes. |
| v1.5.19 | 2026-06-30 | Counselor-only positioning on landing page (next#75, frontend-only). **`src/app/page.tsx`:** Four changes to clarify that Soar is for counselors and IECs, not for students or parents to sign up directly. (1) **Form headline:** "Request access" → "Request access for your practice or school". (2) **Form subtext:** replaced "Soar is currently available by invitation. Tell us about yourself and we'll be in touch." with "Soar is for IECs and school counselors. Students and families join by invitation from their counselor — not through this form. Tell us about your practice and we'll be in touch." (3) **`user_type` dropdown:** removed `<option value="student">Student</option>` and `<option value="parent">Parent</option>`; kept Independent educational consultant, School counselor, and Other only. (4) **Email references:** both `help@lifelaunchr.com` occurrences updated to `help@withsoar.ai` — the success state ("feel free to reach out at help@withsoar.ai") and the error fallback message ("email help@withsoar.ai"). No backend changes. No new components. No new API calls. No new env vars. |
| v1.5.18 | 2026-06-30 | Graceful Claude API error handling — frontend portion (app#163; backend in backend CLAUDE.md v1.5.18). **`src/app/reports/page.tsx`:** New `draftError: string \| null` state (line ~223). `handleDraft` calls `setDraftError(null)` at start. On `!res.ok`, parses `body.detail` (with `.catch(() => ({}))` guard) and calls `setDraftError(body.detail \|\| 'Failed to generate draft — please try again.')`. Error rendered as a `<p>` with `color: #dc2626, fontSize: 0.875rem, marginTop: 8` immediately below the "Draft with AI" button (line ~1681). **`src/components/writing/WritingCoachView.tsx`:** New `essayPlanError: string \| null` state in `StudentAssignmentPanel`. Cleared in four places: student switch `useEffect` (alongside the other plan state resets), generate start (`generateEssayPlan` useCallback), regenerate path (before POST). Populated in three places: (1) `useEffect` initial GET — when `data.last_error` is set and `!data.generating`, calls `setEssayPlanError(data.last_error)` so the error persists when navigating between students; (2) `checkEssayPlanReady` — checks `!data.generating && data.last_error` first (before the `generated_at` check), calls `setEssayPlanError` + clears spinner and not-ready flag; (3) generate POST non-200 response — parses `body.detail` and calls `setEssayPlanError`. Rendered as `<p className="text-sm text-red-600 mt-2">` below the "Not ready yet" message (line ~1795). No new components. No new API calls. No new env vars. |
| v1.5.17 | 2026-06-30 | withsoar.ai domain migration — Phase 2: soar redirect + maintenance mode + branding flash fix (frontend portion; backend portion in backend CLAUDE.md v1.5.17). **`next.config.ts`:** Uncommented and implemented the `soar.lifelaunchr.com → withsoar.ai` Phase 2 redirect. Rule: `source: '/:path((?!sign-in|sign-up|sso-callback|accept-invite).*)'`, `has: [{ type: 'host', value: 'soar.lifelaunchr.com' }]`, `destination: 'https://withsoar.ai/:path*'`, `permanent: false`. The negative lookahead excludes auth paths so Clerk sign-in and invite acceptance continue to work on `soar.lifelaunchr.com`. Switch to `permanent: true` (308) after a few weeks to transfer SEO credit. **`src/proxy.ts`:** Added maintenance mode gate at the top of `clerkMiddleware`. `MAINTENANCE_MODE = process.env.MAINTENANCE_MODE === 'true'` (read from Vercel env var). `maintenancePage()` returns a 503 HTML response with Soar branding: dark background, white card, "We're making a quick update and will be back in a few minutes." Bypass mechanism: `/?bypass={ACCESS_PASSWORD}` (where `ACCESS_PASSWORD` is the existing site password env var) sets a 4-hour `maintenance_bypass` httpOnly cookie; requests with the valid cookie skip the maintenance page. Usage: set `MAINTENANCE_MODE=true` in Vercel Production before pushing; test by visiting the site; visit `/?bypass=PASSWORD` to set bypass cookie; verify redirects; remove the env var when done. **`src/components/chat/ChatHeader.tsx`:** Added `brandingLoaded?: boolean` prop (default `false`). When `false`, neither logo nor text name renders at all (prevents the Soar logo from briefly showing before per-tenant branding is fetched). When `true`, renders logo if `logoUrl` is set, otherwise falls back to `SoarLogo` component or custom bot name. Logo `<img>` height increased from `h-7` to `h-10`. Default tagline updated from "Your AI-powered college advisor" to "College and Career Planning, Built for the Whole Team." **`src/components/chat/ChatInterface.tsx`:** Added `brandingLoaded: boolean` state (initially false). Set to `true` in the `.finally()` block of the `/tenant-config` fetch so it flips regardless of success or error. Default tagline in initial state updated to match. `brandingLoaded` passed as prop to `ChatHeader`. |
| v1.5.16 | 2026-06-26 | Annual plan upgrade preview copy — single combined sentence (app#145, frontend portion). **`src/app/settings/billing/page.tsx`:** The proration preview block for annual plans was two separate `<p>` tags — one for the prorated charge and one for the new recurring total. Rewritten as a single `<p>` with inline conditionals: when `prorationPreview > 0` shows `"$X.XX charged now, then"` inline; when `< 0` shows `"$X.XX credit applied, then"` inline; followed by the new annual total and `"starting [date] and annually thereafter."` Matches the backend's new `proration_amount` + `next_renewal_date` dry_run fields. Backend changes in backend CLAUDE.md v1.5.16. No new components. No new API calls. No new env vars. |
| v1.5.13 | 2026-06-23 | Essay Plan async refactor + persistent generation state (frontend portion, backend in backend CLAUDE.md v1.5.13). **`src/components/writing/WritingCoachView.tsx`:** (1) **`generateEssayPlan`** — replaced SSE reader loop (`getReader()`, buffer decoding, `status`/`done` event handling) with a simple POST that sets `essayPlanGenerating = true` and returns immediately; backend is now authoritative on state. (2) **`checkEssayPlanReady`** (new) — called by "Check if ready" button; hits GET endpoint; if `!data.generating && data.generated_at`, loads plan and clears spinner; otherwise sets `essayPlanNotReady = true` for 4 seconds to show "Not ready yet, try again in a moment" message. (3) **`useEffect` on `student.id`** — resets all essay plan state then calls GET; now handles `data.generating === true` → sets `essayPlanGenerating = true`, restoring the spinner when a counselor navigates away and back while generation is still running. (4) **Removed `essayPlanStartedAt`** state var (was used to detect stale cached plans via timestamp comparison; backend `generating` flag makes this unnecessary). (5) **`essayPlanStatus`** state var retained (still passed as `statusMsg` prop to `EssayPlanPanel`) but no longer updated during generation — always `''`; the `&&` gate inside `EssayPlanPanel` prevents empty renders. (6) UI: spinner + "Generating in background…" message + "Check if ready" button shown when `essayPlanGenerating`; "Not ready yet" inline message shown for 4s after a check that confirms still generating. |
| v1.5.12 | 2026-06-23 | Essay Plan UI — counselor-facing AI analysis overlay in Writing Hub (next#70, frontend-only). **`src/components/writing/WritingCoachView.tsx` (+673 lines, 0 deletions):** All Essay Plan UI is additive to the existing file. (1) **New TypeScript interfaces** added after existing `WritingAssignment` interface: `EssayPlanTab` union type (`'top10' \| 'commonapp' \| 'uc_piqs' \| 'why_major'`); `EssayPlanTop10Item` (`rank`, `label`, `evidence`, `is_essay_anchor`, `source`, `tension`, `_caveat?`); `EssayPlanTop10` (`student_name`, `item_count`, `items[]`, `assignments_to_redo[]`, `opening_question`, `data_quality_note?`); `EssayPlanConcept` (`rank`, `label`, `readiness`, `theme`, `category`, `overcrowded?`, `story_structure?`, `core_claim`, `risk_factors?`, `viability_note?`); `EssayPlanCommonApp` (`student_name`, `data_integrity_note?`, `concepts[]`, `triage_note`, `student_facing_summary`); `EssayPlanPiq` (`piq_number`, `piq_keyword`, `topic_label`, `why_this_student`, `concrete_example`, `reflection`, `forward_connection`, `drift_risk`, `readiness`); `EssayPlanUcPiqs` (`student_name`, `data_integrity_flag?`, `recommended_piqs[]`, `diversity_check`, `triage_note`, `student_facing_summary: string \| string[]`); `EssayPlanWhyMajor` (`student_name`, `intended_major`, `major_confidence`, `major_mismatch_flag?`, `origin_moment?`, `development_arc?`, `intellectual_hook?`, `goals_connection?`, `drift_risk?`, `uc_flag?`, `readiness`, `triage_note`, `student_facing_summary`); `EssayPlanSections` (`top10?`, `commonapp?`, `uc_piqs?`, `why_major?`). (2) **`readinessBadge(r: string)`** helper — returns color-coded JSX chip: green (`ready`), amber (`close`), rose (other/default). (3) **`CopyButton({ text })`** component — copies to clipboard; shows "Copied ✓" for 2 seconds. (4) **`EssayPlanPanel`** full-screen fixed overlay (same dark overlay + right-panel pattern as `ReviewPanel`): `ALL_TABS` typed const filtered to only tabs with present sections; tab bar; status row with generated-at timestamp + Regenerate button; per-tab rendering — Top 10 (anchor flag, tension, source per item, assignments-to-redo, opening question); CommonApp (readiness badge per concept, theme/category/viability, student-facing summary with CopyButton); UC PIQs (PIQ number+keyword, concrete example, reflection, forward connection, drift risk, student-facing summary with CopyButton); Why Major (confidence, four arc components, drift risk, UC flag, student-facing summary with CopyButton). (5) **`StudentAssignmentPanel` changes:** five new state vars (`essayPlan`, `essayPlanGeneratedAt`, `essayPlanGenerating`, `essayPlanStatus`, `showEssayPlan`); `useEffect` on `student.id` fetches `GET /writing/students/{id}/essay-plan` (200 → populate, 404 → clear silently); `generateEssayPlan` useCallback streams `POST /writing/students/{id}/essay-plan/generate` via `getReader()`, handles `status` events (live status bar) and `done` event (set plan + timestamp); Essay Plan card in content area (before "Essay Prompts & Drafts") with generate/regenerate button + status bar + "View Essay Plan →" button; `<EssayPlanPanel>` rendered alongside `AssignPanel` and `ReviewPanel`. Gate: `!readOnly && enabledModules.selfDiscovery`. **TypeScript fix:** `ALL_TABS` typed as `{ key: EssayPlanTab; label: string }[]` before `.filter()` to preserve literal union for safe indexing of `EssayPlanSections`. Backend: see backend CLAUDE.md v1.5.12. |
| v1.5.11 | 2026-06-22 | Resend invite links — admin modal + counselor dashboard drawer (app#160, frontend portion). **`src/app/admin/page.tsx`:** Three changes. (1) Two new state vars: `resendingInvite: boolean`, `resentInvite: boolean`. (2) Both `onClick` handlers that call `setEditingUser({ ...u })` (on the "Invited" badge copy button in the table row and in the tenant-admin user list) now also reset `setResentInvite(false)` and `setResendingInvite(false)` so state clears when switching users. (3) Invite section condition changed from `!editingUser.clerk_user_id && editingUser.invite_url` → `!editingUser.clerk_user_id` so the section always renders for pending users regardless of whether a valid token exists. When `invite_url` is null (expired/never generated): shows amber "Invite link expired or not yet generated." notice + single "Generate & send invite" button. When `invite_url` exists: shows the URL in a monospace box + "Copy Link" button + "Resend invite email" button. Both buttons call `POST /admin/users/{editingUser.id}/resend-invite`; on success, updates `editingUser.invite_url` in local state with the new token URL. **`src/app/dashboard/page.tsx` — `EditPanel` component:** Six changes. (1) Six new state vars: `resendingStudent: boolean`, `resentStudent: boolean`, `parents: { id, full_name, email, clerk_user_id, invite_url }[]`, `parentsLoading: boolean`, `resendingParent: number | null`, `resentParent: number | null`. (2) New `useEffect` (keyed on `student.id`) calls `GET /counselor/dashboard/students/{id}/parents` on drawer open; populates `parents` state; cancellation-safe with `cancelled` flag. (3) Section order corrected: **Invite Link** (student) now appears first, **Parents** section below — prevents the "Generate Invite Link" button from appearing to belong to the parent. (4) Student Invite Link section: "Resend email" button added alongside existing Copy Link + Regenerate buttons; calls `POST /admin/users/{student.id}/resend-invite`, updates `inviteUrl` state with new URL on success. (5) Parents section: hidden when `parents` is empty and not loading. Each parent row shows name + email; signed-up parents get green "Active" badge; pending parents get amber "Invited" badge + "Resend" button (calls `POST /admin/users/{parent.id}/resend-invite`) + clipboard icon to copy invite URL. (6) Expiry date bug fixed: was `fmtDate(inviteExpiry)` which appends `T00:00:00` to an ISO timestamp string producing "Invalid Date"; now uses `new Date(inviteExpiry).toLocaleDateString(...)` directly. Backend: `POST /admin/users/{user_id}/resend-invite` and `GET /counselor/dashboard/students/{student_id}/parents` in backend CLAUDE.md v1.5.11. |
| v1.5.10 | 2026-06-18 | Tutorial videos expansion + SEO metadata for tutorials page (frontend-only). **`src/app/tutorials/page.tsx`:** Added 9 new videos to the `VIDEOS` array after the two existing entries, in this order: `1202638568` (Updating a Student Profile), `1202689764` (Entering Student Activities Lists), `1202638152` (Using Soar to Explore Majors and Careers), `1202638545` (Generating Personalized Soar Summaries for Each College), `1202638285` (Generating Meeting Briefs Before Meetings), `1202638154` (Generating Session Summaries), `1202638153` (How Soar Calculates College Admission Likelihoods), `1202689765` (Generating Activities Lists for Applications), `1202638155` (How Soar Avoids Hallucinations). Total library is now 11 videos. **New `src/app/tutorials/layout.tsx`:** Server component (no `'use client'`) that exports `metadata` for the tutorials route — required because `page.tsx` is a Client Component and Next.js App Router does not allow `export const metadata` in Client Components. Sets `title: 'Tutorials — Soar'`, a tutorials-specific meta description, and full `openGraph` + `twitter` card tags (summary_large_image, `/og-image.png?v=3`, `withsoar.ai/tutorials` canonical URL). Renders `{children}` with a React fragment — no layout chrome. Pattern: any time a client page needs per-page SEO metadata, create a sibling `layout.tsx` in the same directory and export `metadata` from it. No backend changes. No DB migrations. No new env vars. No new endpoints. |
| v1.5.9 | 2026-06-17 | Invite Counselor modal + invited counselor onboarding (app#148, frontend portion). **`src/app/admin/page.tsx`:** Five changes. (1) **State:** five new state vars — `showInviteCounselor: boolean`, `inviteCounselorName: string`, `inviteCounselorEmail: string`, `inviteCounselorSaving: boolean`, `inviteCounselorResult: { status: string, invite_url: string \| null } \| null`. (2) **`submitInviteCounselor` handler:** POSTs to `POST /tenant-admin/counselors/invite` with `{ full_name, email }`; on success calls `setInviteCounselorResult(data)` and appends newly created counselor to local `users` state (using all required `UserRow` fields with sensible defaults + `as UserRow` cast). (3) **`+ Invite Counselor` button** in the Users tab filter row — visible to all `isTenantAdmin` users (including super-admins); indigo `bg-indigo-600` color. (4) **Invite Counselor modal** (`showInviteCounselor && ...`): Name + Email fields, Enter on email field submits, Send Invite / Cancel buttons. Success state shows one of four status labels: `created` → "Invite sent!", `reinvited` → "Invite resent!", `reactivated` → "Account reactivated — invite sent!", `connected` → "Already has an account — added to your practice." Copyable invite URL input shown for all non-connected statuses. Error state shown as red text. Close button dismisses and clears result. Modal placed immediately before the `{/* ── Create / Edit tenant modal ── */}` block. **`src/app/onboarding/page.tsx`:** Three changes. (1) **State:** two new state vars — `isInvitedCounselor: boolean` (default false), `invitedPracticeName: string` (default ''). (2) **Migration effect:** new branch for `resolvedType === 'counselor'` — sets `accountType = 'iec'` (so `isCounselor` becomes truthy), `isInvitedCounselor = true`, `invitedPracticeName = syncData.tenant_name \|\| ''`; calls `setStep(4)` to skip role picker and account save (backend already set tenant + role via the invite); removes old "Counselors and unrecognized types: fall through to show step 1 normally" comment. (3) **Step 4 UI:** when `isInvitedCounselor && invitedPracticeName`, a teal welcome banner renders above the feature cards: "🎉 You've joined [Practice Name]. Your account is all set!" — uses `bg-teal-50 border-teal-200 text-teal-800`. Backend: `POST /tenant-admin/counselors/invite` and `/auth/sync` `tenant_name` field in backend CLAUDE.md v1.5.9. |
| v1.5.8 | 2026-06-17 | Résumé import UI on activities page (app#159, frontend portion). **`src/app/activities/page.tsx`:** Five changes. (1) **New `ProposedActivity` interface** (role, organization, description, uc_category, hours_per_week, weeks_per_year, timing, is_current). (2) **`useRef` added** to React import for the hidden file input. (3) **Five new state vars:** `importInputRef` (HTMLInputElement ref), `importing: boolean`, `importError: string | null`, `importSummary: string | null`, `importProposals: ProposedActivity[] | null`, `importConfirming: boolean`. (4) **Two new handlers:** `handleImportFile` — reads the selected file, POSTs to `/profile/${targetId}/activities/import-resume` as multipart FormData, sets `importSummary` + `importProposals` on success or `importError` on failure; `handleConfirmImport` — POSTs to `/profile/${targetId}/activities/batch`, appends created rows to `activities` state, clears proposal state. (5) **UI changes:** (a) Header button group wrapped in a flex div — hidden `<input type="file" accept=".pdf,.docx,.txt">` wired to `importInputRef`; new "📄 Import Résumé" button triggers `importInputRef.current?.click()`, shows "Reading résumé…" during upload, and sits left of the existing "+ Add Activity" button. (b) Dismissible red error banner (`importError`) inserted above the legend. (c) Proposal panel (`importProposals`) rendered between error banner and legend: header with count + summary message, per-activity cards (role, organization, description, category badge in indigo, hours/week + weeks/year + timing + current status in muted row), "Create all N activities" confirm button + Cancel button + "You can edit any entry afterwards." hint. (d) Empty state gets a second button "📄 Import from Résumé" alongside the existing "+ Add First Activity" button. Backend: `POST /profile/{student_id}/activities/import-resume` and `POST /profile/{student_id}/activities/batch` in backend CLAUDE.md v1.5.8. |
| v1.5.7 | 2026-06-17 | Admin Add Parent modal (app#158, frontend portion). **`src/app/admin/page.tsx`:** Three changes. (1) **State:** three new state vars — `addParentStudent: UserRow | null` (which student the modal is open for), `addParentName: string`, `addParentEmail: string`, `addParentSaving: boolean`, `addParentResult: { status: string, invite_url: string | null } | null`. (2) **`submitAddParent` handler:** POSTs to `POST /admin/students/{addParentStudent.id}/add-parent` with `{ full_name, email }`; sets `addParentResult` on both success and failure. (3) **UI:** Green `+ Parent` button added to each student row's action cell (visible only when `isAdmin || isSuperAdmin` and `u.account_type === 'student'`). Edit button and + Parent button wrapped in a flex div. New `addParentStudent && (...)` modal: name + email fields (Enter key submits), submit/cancel buttons, disabled while saving. After submit: shows one of four status messages (Invite sent / Invite resent / Account reactivated — invite sent / Already has an account — notification sent) with a copyable invite-URL input when applicable. Error state shown as red text. Backend: `POST /admin/students/{student_id}/add-parent` in backend CLAUDE.md v1.5.7. |
| v1.5.5 | 2026-06-16 | Transcript Drawer overhaul — GPA sections, A-G chips, notable flags, counselor instructions (frontend-only, transcript analysis improvements). **`src/app/profile/TranscriptDrawer.tsx`:** Six display improvements corresponding to the backend v1.5.5 transcript analysis fixes. (1) **Separate Overall GPA and UC/CSU GPA sections:** GPA table split into two rows — Overall GPA (all weighted/unweighted combos) and UC/CSU GPA (calculated from transcript using UC methodology). Each row shows the value from `student_info.gpa_overall` and `student_info.gpa_uc_csu` respectively. (2) **A-G status chips on subject rows:** Each Core Subject Overview row now shows a colored chip indicating "On track" (green), "Plan needed" (amber), or "Concern" (red) alongside the completed/projected year counts. Chip styling matches the `StatusChip` component with hover tooltips. (3) **G elective note:** When a student has elective credits counted under the G (College-Preparatory Elective) requirement, a note is displayed explaining that G-elective credits from the transcript have been counted toward the 1-year requirement. (4) **Notable flags section:** New section above the existing Flags section displays `analysis.notable_flags` entries — honor awards (NMS Commended, NHS, etc.), class rank, disciplinary notes, or other notable items. Each flag renders as a colored chip: purple background for `honor` (★ icon), blue for `class_rank` (# icon), red for `disciplinary` (⚠ icon), grey for `other` (• icon). TypeScript interface: `notable_flags?: Array<{ type: 'honor' \| 'class_rank' \| 'disciplinary' \| 'other', description: string }>`. Only rendered when array is non-empty. (5) **Counselor instructions UI:** New expandable section at the bottom of the drawer for counselors only (not shown to students) displaying `analysis.counselor_instructions` — the recommendations Claude generates about what the counselor should discuss or flag with the student. Shows as an amber-tinted collapsible block with "Counselor notes" header. (6) **`profile/page.tsx`:** Minor wiring changes to pass updated analysis fields through to the drawer component. Backend changes (grade-based completion detection, LOTE mapping fix, Math in-progress fix, notable_flags JSON field, counselor instructions, current_grade bump rule, subject projection status post-processing) in backend CLAUDE.md v1.5.5. No DB migrations. No new API endpoints. No new env vars. 105/105 wiring checks passed. |
| v1.5.1 | 2026-06-12 | Fix /accept-invite on withsoar.ai satellite domain (frontend-only). **Root cause:** invite emails sent to Ranna Patel's 25 families used `withsoar.ai/accept-invite?token=...` links which failed with a Clerk error ("Production Keys are only allowed for domain 'lifelaunchr.com'") because the `accept-invite` page initializes Clerk.js, which cannot run on a satellite domain with the primary domain's publishable key. **Fix 1 — `src/proxy.ts`:** Added `/accept-invite` to the withsoar.ai redirect block so the middleware redirects `withsoar.ai/accept-invite?token=...` → `soar.lifelaunchr.com/accept-invite?token=...` (query string preserved via `url.search`). **Fix 2 — `next.config.ts`:** Added two host-based redirects (`has: [{type: 'host', value: 'withsoar.ai'}]` and `www.withsoar.ai`) for `/accept-invite` → `https://soar.lifelaunchr.com/accept-invite` as belt-and-suspenders (next.config redirects run at a different layer than middleware). **Fix 3 — `src/proxy.ts` and `src/app/layout.tsx`:** Corrected `domain: 'lifelaunchr.com'` → `domain: 'withsoar.ai'` in both the `clerkOptions` function and `ClerkProvider` satelliteProps. The `domain` prop tells Clerk "what domain am I currently on?" — having the primary domain set while `isSatellite: true` was the root cause of the Clerk JS error. **Note — Cloudflare redirect:** A Cloudflare Single Redirect Rule was also created (URI Path equals `/accept-invite` → `https://soar.lifelaunchr.com/accept-invite`, 302, preserve query string) but is **ineffective** because withsoar.ai DNS is in "DNS only" mode (A `@` → Vercel IP, orange cloud off) — Cloudflare rules only fire when traffic proxies through Cloudflare. The rule exists but does nothing; the Next.js-level fixes are what actually work. No DB migrations. No new API endpoints. No new env vars. Also added `scripts/clerk_allowlist_ranna.py` to backend repo — one-shot script to add Ranna's 74 family emails to the Clerk allowlist (all were already added by the original batch import, confirmed by `duplicate_record` responses). |
| v1.5.0 | 2026-06-12 | withsoar.ai domain launch (backend + frontend, next#68). **`src/proxy.ts`:** Added `clerkOptions` async function as second arg to `clerkMiddleware` — detects withsoar.ai Host header and returns `{ isSatellite: true, domain: 'lifelaunchr.com', signInUrl: 'https://soar.lifelaunchr.com/sign-in' }`, or `{}` for all other hosts. This handles server-side Clerk auth for the satellite domain without affecting soar.lifelaunchr.com. Also added: when on withsoar.ai, middleware redirects `/sign-in` and `/sign-up` to `soar.lifelaunchr.com` (satellite domains do not host their own sign-in page). **`src/app/layout.tsx`:** Made `RootLayout` async; added `import { headers } from 'next/headers'`; reads `host` header on every request and spreads `{ isSatellite: true, domain: 'lifelaunchr.com', signInUrl: 'https://soar.lifelaunchr.com/sign-in' }` into `ClerkProvider` when on withsoar.ai. Required because Clerk's browser JS validates the publishable key domain independently of the middleware. **`next.config.ts`:** Updated `lifelaunchr-next.vercel.app` redirect destination to `https://withsoar.ai`. Added soar.lifelaunchr.com → withsoar.ai redirect as a **commented-out** block (enable in Phase 2 — see next#68). **`src/app/robots.ts`:** Sitemap URL updated to `https://withsoar.ai/sitemap.xml`. No new components. No DB migrations. No new API endpoints. No new env vars in code (env vars updated directly in Render/Vercel). See backend CLAUDE.md v1.5.0 for backend changes and full infrastructure record. |
| v1.4.9 | 2026-06-12 | Tutorials page + sidebar nav refactor + CLS layout-shift fix (frontend-only). **New page `src/app/tutorials/page.tsx`:** public (no auth gate), no `middleware.ts` changes needed. Client component with two states: gallery view and single-video player. `VIDEOS` config array at the top — add `{ id, title, description }` to grow the library; thumbnails fetched automatically on mount via `GET vimeo.com/api/oembed.json?url=https://vimeo.com/{id}&width=640` (works for unlisted videos; falls back to a dark gradient placeholder if the API call fails). Gallery renders as a responsive CSS grid (`grid-cols-1 sm:grid-cols-2 lg:grid-cols-3`). Each card: aspect-video thumbnail with a white play-button overlay, title, description. Clicking a card enters the player view (full-height iframe, `autoplay=1`). Header shows "← All tutorials" back button in player mode, title + tagline in gallery mode, and an "Open Soar →" link always. Fallback: when `VIDEOS` is empty, shows the Vimeo showcase embed (`autoplay=0`). Two videos pre-populated: `1197542498` ("Why Soar is Different") and `1198259270` ("Invite Your First Family"). Route is accessible at `/tutorials` without login. **`src/components/chat/ChatInterface.tsx` — sidebar nav refactor:** Three areas changed. (1) **CLS layout-shift fix:** `isCounselor` and `isParent` `useState` initializers now read `localStorage.getItem('ll_account_type')` synchronously on first paint (SSR-safe: guarded by `typeof window === 'undefined'`). `fetchUsage` saves `accountType` to `localStorage.setItem('ll_account_type', accountType)` after setting state. The user-switch `useEffect` now also calls `localStorage.removeItem('ll_account_type')` when `userId` changes. Root cause: both values started `false` and flipped after `fetchUsage()` resolved, causing role-specific nav items to pop in and shift everything below on every page load for return users. (2) **Nav reorder:** nav block reorganized into four logical sections separated by dividers — *Controls* (student picker, Add Family, invite link, schedule button), *Main nav* (My Students for counselors; My Lists + My Activities for students; Session & Research Summaries for all; Writing when module-gated), *Student sub-nav* (thin divider + "STUDENT" label + Student Profile + Lists + Activities — only when counselor/parent has a student selected), *Tools* (Tutorials → My Info/Profile → Admin Dashboard → Billing & Plan). (3) **Compaction + cleanup:** all nav links changed to `py-1.5 text-xs` (down from `py-2.5 text-sm`); emoji icons changed to `text-sm` (down from `text-base`); the username display row (`clerkUser.fullName`) and its surrounding divider removed (~35 px recovered); nav container wrapped in a `relative` div to position the bottom gradient hint (`pointer-events-none absolute bottom-0 h-10 bg-gradient-to-t from-[#1a1a2e] to-transparent`) — this fade visually cues that the nav is scrollable when tall. No backend changes. No DB migrations. No new env vars. |
| v1.4.8 | 2026-06-11 | Multi-transcript support — complete rewrite of `TranscriptDrawer.tsx` + profile page wiring (backend + frontend). **`src/app/profile/TranscriptDrawer.tsx` — complete rewrite:** New `TranscriptMeta` interface (`id`, `institution_name`, `institution_type`, `is_primary`, `file_name`, `file_size_bytes`, `uploaded_at`). New `SubjectProjection` interface (`subject`, `status`, `completed_years`, `projected_years`, `rigor_observation`, `courses`). `TranscriptAnalysis` updated: new fields `transcript_type` (`"us_standard" \| "international"`), `international_narrative` (string), `subject_projection` (array of 6 universal subjects). New `StatusChip` component with hover tooltip — each chip shows "On track" (green), "Plan needed" (amber), or "Concern" (red) with a `STATUS_COPY` dict driving tooltip text per status. New `CourseList` component showing 3 courses then "+N more" toggle. New `instTypeBadge(type)` helper rendering colored badge (High School / Comm. College / Online / Other). Props changed: `transcripts: TranscriptMeta[]` (replaces single transcript state), `onDeleteTranscript(id: number)` (per-ID delete), `sameInstitutionWarning: boolean`, `onDismissSameInstitutionWarning()`. Layout: same-institution amber warning banner (dismissible) → transcript list with per-item delete → "Add another transcript" button → international narrative section (replaces GPA for international transcripts) → student info → GPA table + **GPA disclaimer** (links to https://hs-articulation.ucop.edu/agcourselist for UCOP A-G course honors data; explains UC/CSU vs. all-other-students methodology; labels as guidance not official) → Grade Trend → Core Subject Overview (6 universal subjects; appears for all transcript types) → UC/CSU A-G Requirements (relabeled "California") → Flags. Footer: "Re-analyze all" button only (no upload/delete in footer). **`src/app/profile/page.tsx`:** import updated to `import TranscriptDrawer, { type TranscriptMeta } from './TranscriptDrawer'`. New state `transcripts: TranscriptMeta[]` and `sameInstitutionWarning: boolean`. Profile load `useEffect` fetches `GET /profile/${targetId}/transcripts` on load and sets `transcripts`. `handleTranscriptUpload` updated: reads `data.transcripts` (new) and `data.same_institution_warning`; updates both states. New `handleTranscriptDeleteById(transcriptId: number)` calls `DELETE /profile/${targetId}/transcripts/${transcriptId}`, updates `transcripts` state, triggers re-fetch of `transcript_analysis`. Old `handleTranscriptDelete` removed. Inline UI: shows "N transcripts analyzed" (plural) or "Transcript analyzed" (singular) based on `transcripts.length`; upload button label changed to "+ Add transcript". `TranscriptDrawer` JSX updated to pass all new props. Backend changes (multi-transcript storage, combined analysis, fuzzy institution matching, new endpoints) in backend CLAUDE.md v1.4.8. |
| v1.4.6 | 2026-06-08 | Session report AI draft — student_id wiring (app#141, frontend portion). **`src/app/reports/page.tsx`:** `student_id: formStudentId || null` added to the draft POST body (`generateDraft` function). This one-line change passes the student's DB user ID to the backend so it can fetch the student's current college, scholarship, and enrichment lists and inject them as context into the AI prompt. Previously, `student_id` was not sent and the backend had no way to look up the lists. Backend changes (transcript cap, max_tokens, prompt rules, writing privacy, list injection) in backend CLAUDE.md v1.4.6. |
| v1.4.5 | 2026-06-08 | Research session untag + mobile streaming fix (next#52 partial, frontend portion). **`src/components/chat/ChatInterface.tsx`:** Three new state vars: `showUntagConfirm: boolean`, `untagging: boolean`, `untagSuccess: boolean`. New `useEffect` resets `showUntagConfirm` and `untagSuccess` whenever `currentResearchSessionId` changes (session navigation). New `handleUntag` `useCallback`: POSTs to `/research-sessions/{currentResearchSessionId}/untag`; on 200: (1) sets `untagSuccess = true` and closes modal; (2) optimistically removes the current session from sidebar state — `setSessions(prev => prev.filter(s => s.id !== activeSessionId))`; (3) clears the chat panel to welcome card — `setMessages([])`, `setConversationHistory([])`, `setServerSessionId(null)`, `setActiveSessionId(null)`, `setCurrentResearchSessionId(null)`; (4) calls `fetchSessions()` to confirm server state. On error, shows `summaryToast` with backend error detail. Deps: `[currentResearchSessionId, untagging, getToken, apiUrl, fetchSessions, activeSessionId]`. In the active-student banner (`isCounselor` only), a new "· This was my own research" link appears between "Generate a summary of this session" and "× Clear" — only when `currentResearchSessionId && messages.length > 0 && !untagSuccess`. On success, replaced by a green "✓ Session removed from [Name]'s research" span. New amber confirmation modal (same fullscreen overlay pattern as `showSummaryConfirm`): header "Remove student from this session?", body "This will remove [Student Name] from this session and count it toward your own research. This cannot be undone.", amber "Remove from student" button with spinner during `untagging`, Cancel. **Mobile streaming fix:** explicit `if (!response.body) throw new Error('response.body is null — browser streaming not supported')` guard before `response.body.getReader()` (replaces silent `TypeError` from the `!` non-null assertion). `console.error('[chat stream error]', err)` added to the outer `catch` block so stream errors appear in DevTools. These changes were added after diagnosing a mobile-specific failure: mobile Chrome was silently dropping SSE connections during long tool-call gaps (backend fix: keepalive 15 s → 5 s, in backend CLAUDE.md v1.4.5). Backend changes (new endpoint + DB column + dual-path chat_sessions clear + keepalive fix) in backend CLAUDE.md v1.4.5. |
| v1.4.4 | 2026-06-08 | Writing Hub submitted draft visibility (next#67, frontend-only). **`src/app/writing/assignment/[id]/page.tsx`:** Four fixes. (1) `handleSubmitForReview` now captures the POST `/responses` response data and updates `assignment.responses` in local state (same pattern as `handleSave`) — root cause fix: previously `responses` stayed empty after submit, so the Drafts tab never appeared and the hint pointed to a nonexistent tab. (2) After a successful submission, `setActiveTab('history')` is called so the student lands on their submitted work immediately. (3) Write tab submitted-state: grey unclickable `<p>Check the Drafts tab…</p>` replaced with a violet `<button>View your submission →</button>` that calls `setActiveTab('history')`; only rendered when `responses.length > 0`. (4) Drafts tab: removed `line-clamp-3` (free-write) and `line-clamp-2` (structured fields) so full submission text is always readable; renamed "Restore this draft →" to "Edit & resubmit →" to clarify intent. No backend changes. |
| v1.4.3 | 2026-06-07 | Writing Hub unassign exercise + content visibility (next#66, backend + frontend). **`src/components/writing/WritingCoachView.tsx`:** Changes across three areas. (1) `AssignPanel` — unassign: `previouslyAssigned: Set<number>` and `newlyAssigned: Set<number>` replaced with `assignedMap: Map<number, number>` (exerciseId → assignmentId). Initial load populates map from existing assignments. `assign()` captures `data.assignment.id` from POST response and stores in map. New `unassign(exerciseId, force?)` calls `DELETE /writing/assignments/{assignmentId}`; on 200 removes from map and calls `onAssigned()`; on 409 sets `unassignWarning` to show amber "Unassign anyway · Cancel" warning. New `unassigning: number | null` and `unassignWarning: number | null` states. Assigned exercise cards show "Unassign" button (grey → red on hover) instead of "✓"; card opacity changed from `opacity-50` to text-only dimming so the button remains visually interactive. (2) `AssignPanel` — content visibility: `LibraryExercise` interface gains `framing_content: string | null`. Cards with no `prompt_text` now show a "Show content / Hide content" violet toggle that expands the full `framing_content` inline (same `expandedId` state). Cards with `prompt_text` keep existing "Read more / Show less" behavior. (3) `StudentAssignmentPanel` — unassign: New `unassignAssignment(id, force?)` function + `unassigningRowId: number | null` + `unassignRowWarning: number | null` states. Assignment list rows for `status !== 'reviewed'` gain a small "Unassign" button next to View/Review. Same 409 warning pattern. (4) `WritingAssignment` interface gains `framing_content: string | null`. `ReviewPanel` PROMPT section extended to also handle content-type exercises: when `prompt_text` is null, shows `framing_content` under a "CONTENT" label instead of "PROMPT". Backend: `GET /writing/library` now returns `framing_content`; new `DELETE /writing/assignments/{id}` endpoint — see backend CLAUDE.md v1.4.3. |
| v1.4.2 | 2026-06-07 | Writing Hub prompt visibility fixes (next#65, frontend-only). **`src/components/writing/WritingCoachView.tsx`:** Two changes. (1) `AssignPanel` — new `expandedId: number | null` state; each exercise card with a `prompt_text` now renders a "Read more" / "Show less" violet toggle link below the clamped text; clicking it toggles `line-clamp-2` off/on for that card only; `e.stopPropagation()` prevents the click from bubbling to any parent handler. (2) `ReviewPanel` — new PROMPT section inserted between the panel header and STUDENT RESPONSE; renders `assignment.prompt_text` in a `bg-slate-700/20` rounded box with the standard small-caps label style; always visible (not collapsible) so the counselor immediately sees the exercise question when reviewing a student submission or checking an unstarted assignment. No backend changes. |
| v1.4.1 | 2026-06-07 | Research session data-integrity fix (app#140, frontend portion). **`src/components/chat/ChatInterface.tsx`:** added a new `useEffect` for counselors (mirrors the existing parent stale-check at line ~520). When `isCounselor` is true and `myStudents` has loaded (non-empty), checks if `forStudentId` is still in the student list. If not (student was archived, removed, or the value came from a different account sharing localStorage), sets `forStudentId` to `null` and removes `ll_for_student_id` from `localStorage`. Prevents the stale value from being sent to `/chat` where it would contaminate research session data. Backend changes (validation in `/chat` endpoint + data cleanup) in backend CLAUDE.md v1.4.1. |
| v1.4.0 | 2026-06-06 | Stripe billing v2 — frontend (backend portion in backend CLAUDE.md v1.4.0). **New page `src/app/settings/billing/page.tsx`:** tenant-admin-only billing management page with dark header matching profile page. Shows current plan summary (billing interval, students on plan, active students, monthly total, next billing date). UPDATE PLAN SIZE section: number input (min = active_student_count), "Preview change" button calls `/billing/update-student-count` with `dry_run: true` to get Stripe-calculated new total + renewal date; confirmation message shows "No charge today. New monthly total of $X/mo starting [date]" (monthly) or proration charge + new annual total (annual); "Confirm update" commits the change. PAYMENT & INVOICES section: "Manage billing →" button opens Stripe Customer Portal via POST to `/billing/portal`. Over-limit warnings shown for both subscribed (active > plan limit) and free plan states (active > 3). Accessible from chat sidebar "Billing & Plan" link (tenant admins only). **`src/components/billing/CounselorCheckout.tsx`:** Uses `is_subscribed` flag from `/billing/subscription-status` (not plan name) to detect existing subscription — avoids false positive for migrated counselors with non-starter DB tiers. Shows green "active subscription" banner with "Manage subscription →" link for already-subscribed users. Subscribe button disabled when `count < activeStudentCount`. Fetches live graduated tier pricing from `/billing/price-tiers`. **`src/app/upgrade/page.tsx`:** "Active students: Unlimited" → "Based on plan"; removed "Annual billing (20% off)" add-on (annual is built into checkout toggle). **`src/app/admin/page.tsx`:** `TenantRow` interface gains `active_student_count` and `max_students`; amber ⚠️ "Over limit (active/max)" badge shown under tenant name when over limit. **`src/components/counselor/AddFamilyModal.tsx`:** `PRACTICE_AT_CAPACITY` error for tenant admins now shows "Update your plan →" link to `/settings/billing` instead of generic "Contact your practice admin" message. **GitHub issue next#64 filed:** create shared route-group layout for standalone pages (profile, billing, activities). |
| v1.2.0 | 2026-05-26 | Writing Hub assign panel fix (next#59, frontend-only). **`WritingCoachView.tsx` line 1001:** removed `setAssignPanel(null)` from the `onAssigned` callback. Panel now stays open after each exercise is assigned so counselors can assign multiple exercises in one session; closes only via the explicit X button. `loadData()` still fires after each assign to refresh checkmarks. Backend portion of next#59 (sort order DESC→ASC) in backend CLAUDE.md v1.2.0. |
| v1.1.8 | 2026-05-25 | College list awareness in WelcomeCard (next#120, frontend + backend). **`ChatInterface.tsx`:** new `studentCollegeCount` state; `useEffect` fetches `GET /lists/{myDbUserId}` when `myDbUserId` is set and user is a student (not counselor or parent); result stored as `data.research?.length ?? 0`; passed to `WelcomeCard` as `collegeCount` prop. **`WelcomeCard.tsx`:** new `collegeCount?: number` prop; `hasCollegeList` computed as `accountType === 'student' && (collegeCount ?? 0) > 0`; when true, the "I'm starting my college search — help me figure out where to begin" (first-session) and "Help me find colleges that fit my profile" (returning-user) chips are swapped for "Walk me through my college list — let's figure out where to start". Backend changes (system prompt instruction in `_build_student_context`) in backend CLAUDE.md v1.1.8. |
| v1.1.7 | 2026-05-24 | Student onboarding pre-population for invite-link students (next#119, frontend-only). **`src/app/onboarding/page.tsx`:** when a student arrives via counselor invite link, the onboarding flow now fetches the counselor-created data and surfaces it at the right steps. Three new state variables: `onboardingUserId` (DB user id, set from `/accept-invite` response), `existingCollegeCount` (number of colleges already on the student's list), `navigating` (brief lock during Step 2→3 transition). **Step 2 (profile) pre-population:** in the migration `useEffect`, after `acceptRes.ok`, `acceptedUserId` is now hoisted as a local variable. For `resolvedType === 'student'`, the flow calls `GET /profile/{userId}` with the auth token before `setStep(2)`; if the counselor already filled in graduation year, home state, GPA, SAT, ACT, or intended majors, those fields are pre-populated in the form state. All non-fatal (empty-catch); brand-new students with no counselor data see the current blank-form UX unchanged. **Step 3 (college wishlist) contextual message:** `handleProfileNext` changed from sync to async; if `onboardingUserId` is set, it fetches `GET /lists/{userId}` before advancing to Step 3 and stores the college count in `existingCollegeCount`. When `existingCollegeCount > 0`, the Step 3 heading changes to "Anything to add to your list?" with subtitle "Your counselor has already set up a research list for you — N colleges so far. Any you want to add or make sure are on there?" and the empty-state message changes to "Your existing list will be ready to explore when you start chatting." Step 2 Next button shows "Loading…" and is disabled during the micro-fetch. `headerSubtitle` for step 3 also switches to "Your counselor already added colleges to your list." when count > 0. **Step 6 (starter questions):** `getStarterQuestions()` updated — `hasColleges` now includes `existingCollegeCount > 0`. When `hasCounselorList` is true, the first chip is always "Walk me through my college list and help me figure out where to start"; remaining chips are college-specific if the student added new ones in Step 3, or generic list-exploration chips otherwise. Original chip logic for students who added their own colleges (no counselor list) is unchanged. No backend changes. |
| v1.1.6 | 2026-05-23 | Backend-only: "In summary," closing rule for substantive responses (app#121/122) — see backend CLAUDE.md v1.1.6. No frontend changes. |
| v1.1.5 | 2026-05-23 | Backend-only: in-chat admissions likelihood tool (app#128) — see backend CLAUDE.md v1.1.5. No frontend changes. |
| v1.1.4 | 2026-05-21 | Add Family button in chat sidebar + counselor tour step (next#58, frontend-only). **`ChatInterface.tsx`:** replaced "Copy student invite link" button for counselors with an **Add Family** button (`id="tour-add-family"`). On first click, fetches `/tenant-admin/counselors` (response shape: `{ counselors: [...] }`) and stores result in `counselorOptions` state (`undefined` = not yet fetched; `null` = fetched, regular counselor; array = fetched, tenant admin with counselors list). `setShowAddFamily(true)` is called after the fetch in the same async handler so React 18 batches both state updates into one render. `AddFamilyModal` is now **conditionally rendered** (`{showAddFamily && <AddFamilyModal open={true} .../>}`) so its `useState` initializers run at mount time when `counselorOptions` is already set — previously the modal was always in the DOM and `isTenantAdmin` was permanently `false` because `useState` runs once at mount (before any fetch). `onSuccess` refreshes the sidebar student list via `/my-students`. Invite link button now gated to `!isCounselor` (parents/students only). **`tourContent.ts`:** added "Invite your families" step (target `#tour-add-family`) to counselor tour immediately after the student selector step; updated counselor showcase Dashboard card description. **Two-bug fix note:** (1) conditional render fix ensures modal state initializes correctly; (2) the counselor dropdown was also missing because the fetch handler read `list.length` on the raw response object `{ counselors: [...] }` instead of `data.counselors` — so `counselorOptions` was always set to `null` and `isTenantAdmin` never became true. Fixed by `const list = data.counselors \|\| []`. |
| v1.1.3 | 2026-05-21 | Fix college list status and likelihood bugs (app#126, frontend portion). **`lists/page.tsx`:** added `considering: '#7c3aed'` to `STATUS_COLORS` and `considering: 'Researching'` to `STATUS_LABELS` as legacy aliases — legacy production rows with `status='considering'` now display correctly instead of showing a grey border and raw "considering" label. Backend changes (run_migrations fixes + add endpoint name lookup + compare_schemas.py script) in backend CLAUDE.md v1.1.3. |
| v1.1.2 | 2026-05-21 | Backend-only: fix Writing Hub redirect rules blocking activities lists and résumés (app#127) — see backend CLAUDE.md v1.1.2. No frontend changes. |
| v1.1.1 | 2026-05-19 | Writing Hub back-navigation reliability fix. `router.push + router.refresh` is not reliable in Next.js 14 production builds — the router reads from cache synchronously before refresh can clear it. Replaced all three back-navigation handlers in `writing/page.tsx` and both navigation calls in `writing/assignment/[id]/page.tsx` with `window.location.href`, which bypasses the client router entirely. Also: Review/View action badges on `AssignmentCard` in coach mode; View button shown for all assignment statuses (not just submitted) in `WritingCoachView.tsx`; full coach view added to assignment detail page. No backend changes. |
| v1.1 | 2026-05-19 | Writing Hub production release + coach view fixes. **Back-navigation fix (65c9603):** three `router.push('/writing')` calls in `writing/page.tsx` now also call `router.refresh()` immediately after — the Next.js 14 App Router caches same-pathname navigations with different search params, causing the back button to appear to do nothing; `refresh()` busts the cache. **Coach view on assignment detail page (fe17947):** `/writing/assignment/[id]/page.tsx` previously rendered student UI to all viewers. Added full coach branch: when `isCounselor && forParam`, the page shows a read-only student response, a streaming Soar Observations panel (SSE from `POST /writing/assignments/{id}/generate-observations`), a coach note textarea + save button, and a Mark as Reviewed button (shown when status is `submitted` or `in_progress`). State variables added: `accountType`, `coachResponses`, `coachNote`, `coachSaving`, `coachSaved`, `coachMarking`, `observations`, `generatingObs`. The coach branch returns early so coaches never see the student write tab or "Continue writing →" button. **Review/View button in WritingCoachView (5b7d5c4):** `StudentAssignmentPanel` in `WritingCoachView.tsx` previously gated the Review/View button on `submitted | in_progress | reviewed` — assignments with status `assigned` had no button. Condition removed; button now shows for all statuses. **Review/View badge on AssignmentCard (38f7993):** in `writing/page.tsx`, `AssignmentCard` is a full-row `<Link>` — there was no visible action label in coach mode. Added a small badge to the right of the status label when `studentId` is set (coach mode): `Review →` (violet) for submitted, `View →` (muted) for all other statuses. Content and milestone exercise types excluded (they have their own status displays). Two backend bug fixes in backend CLAUDE.md v1.1 (parent 403 on essays page, parent draft access). |
| v1.0.38 | 2026-05-18 | Writing Hub complete (next#52, next#53). **New `/writing` page** (`src/app/writing/page.tsx`, 2180 lines): tabbed hub for self-discovery, writing practice, and essay sections. Sections visible depend on `enabled_modules`. **Student view:** shows assigned exercises in each section with status badges (`assigned`/`in_progress`/`submitted`/`reviewed`); clicking an assignment opens a full response editor with a word-count bar, revision history dropdown, and submit button. Personality assessment tab (Self-Discovery): 120-item IPIP-NEO questionnaire with per-domain result bars and facet breakdown. **Counselor view:** `WritingCoachView` component — student roster dropdown; for each student shows sections with enrollment toggles and an `AssignPanel` for assigning exercises; when a student has submitted a response the counselor sees the submitted text and an "Generate Observations" button that streams AI coaching notes. **Parent view:** read-only section list with progress counts. **`WritingWelcomeModal`** (`src/components/writing/WritingWelcomeModal.tsx`): role-specific welcome modal shown once per role per browser (`localStorage` keyed by role). Three roles have distinct copy: student (process ownership), counselor (workflow + pacing), parent (privacy + read access). **`AssignPanel`** (`src/components/writing/AssignPanel.tsx`): fetches exercises via `GET /writing/library?section_key=`, renders a list with exercise titles and prompts; "Assign" button POSTs to `POST /writing/assignments`; optional note and due date fields. **`WritingCoachView`** (`src/components/writing/WritingCoachView.tsx`): counselor-only full-page roster view rendered when no student is selected; lists all connected students with assigned/submitted counts; clicking a student switches to that student. **Chat sidebar** (`ChatInterface.tsx`): Writing Hub link (`/writing`) added to sidebar nav for all roles when any writing module is enabled; counselors see it in the student sub-nav. **Essays nav link** restored for counselors and parents: appears in the student sub-nav when `essay_list_module || essays_module` is true; was accidentally removed during Writing Hub work. **`/essays` page restyle** (`src/app/essays/page.tsx`): completely converted from all-inline-styles white theme (775 lines, `style={{...}}` throughout) to Tailwind dark navy matching the Writing Hub — `bg-slate-900`, `bg-slate-800/50`, `border-slate-700/50`, violet-600 buttons, slate text variants, violet/amber/green status badges. All logic preserved identically. Back link now points to `/writing` (or `/writing?for=studentId`). **Dashboard edit panel** (`dashboard/page.tsx`): added student writing access checkboxes — three booleans (`commonapp_essays_enabled`, `uc_piqs_enabled`, `why_essays_enabled`) that toggle which essay hub sections are visible for a student; stored in `users` table. Backend changes in backend CLAUDE.md v1.0.38. |
| v1.0.36 | 2026-05-11 | Sortable college list — clickable spreadsheet headers + sort dropdown (next#51, frontend-only). **`src/app/lists/page.tsx`:** added `CollegeSortBy` type, `LIKELIHOOD_RANK` map, `STATUS_ORDER` array, and `sortCollegeEntries()` pure function (sorts by likelihood tier/name/deadline/status with asc/desc; deadline nulls always last; alpha tiebreak on ties). `SpreadsheetView` gains `sortBy`, `sortDir`, `onSort` props; College, Status, Likelihood, Deadline headers are clickable — active column highlights indigo with ▲/▼ arrow, inactive columns show ⇅. Sort dropdown added to toolbar (colleges tab only, left of view toggle) with 7 options: Likelihood best-fit/reach-first, Name A-Z/Z-A, Deadline soonest/latest, Status. Dropdown and column headers share the same state (`collegeSortBy`, `collegeSortDir`) in `ListsPage`. Default: likelihood asc. Preference persisted to `localStorage` (`ll_college_sort_by`, `ll_college_sort_dir`). Both `SpreadsheetView` and `CardView` receive pre-sorted entries; card view buckets remain grouped (researching/applying) but cards sort within each bucket. No backend changes. |
| v1.0.35 | 2026-05-11 | Friendly error states for inaccessible or missing sessions (next#47, frontend-only). **`src/components/chat/ChatInterface.tsx`:** added `sessionAccessError` state typed as `'forbidden' \| 'not_found' \| null` (replaces previous boolean). `loadSession` now sets `sessionAccessError('forbidden')` on 403 and `'not_found'` on 404 before returning early (previously both were silently swallowed). `handleNewConversation` clears the error on new session. In the render tree, when `sessionAccessError` is non-null an error panel is shown instead of the WelcomeCard: 🔒 icon + "You don't have access to this session" / "This session belongs to a student you're not connected to." for 403; 🔍 icon + "Session not found" / "This link may be broken or the session may have been deleted." for 404. Both panels include a "Start a new session" button. No backend changes. |
| v1.0.34 | 2026-05-11 | Backend-only: enrich Soar Summary with culture/community fit paragraph and deeper academic program analysis (app#117) — see backend CLAUDE.md v1.0.34. No frontend changes. |
| v1.0.33 | 2026-05-11 | Fix college list status 'considering' bug (next#50, frontend portion). **`src/app/lists/page.tsx` card view (`CardView` component):** replaced fragile negative filter `e.status && e.status !== 'researching'` with explicit allowlist `APPLYING_STATUSES = ['applying','applied','accepted','denied','waitlisted','deferred','withdrawn','decided']`. Added 'considering' as a fallback alias for 'researching' in the researching bucket. Fixed the "X applying" summary count to use the same allowlist so 'considering' entries no longer inflate the applying count. Backend changes in backend CLAUDE.md v1.0.33. **Data fix required on staging and production:** `UPDATE college_list SET status='researching', updated_at=NOW() WHERE status='considering';` |
| v1.0.32 | 2026-05-11 | Backend-only: fix Meeting Brief silently excluding unsummarized research sessions (app#116) — see backend CLAUDE.md v1.0.32. No frontend changes. |
| v1.0.31 | 2026-05-11 | Auto-expand textareas in college drawer Research and People tabs (next#49, frontend-only). **`src/app/lists/page.tsx`:** added `autoExpand` `useCallback` inside `EditDrawer` (sets `height = 'auto'` then `height = scrollHeight + 'px'`). Applied to all 9 note textareas: 5 in the Research tab (academic_notes, cultural_notes, admission_notes, other_interesting_facts, relevant_merit_scholarships) and 4 in the People tab (coach_notes editable, coach_notes readonly, parent_notes, student_note). Each textarea gets `ref={el => autoExpand(el)}` (fires on drawer open to expand existing content) and `onInput={e => autoExpand(e.currentTarget)}` (expands live as the user types). `rows` attribute removed; replaced with `minHeight: '72px'` (3-row fields) or `minHeight: '90px'` (coach notes, was 4 rows); `resize: 'vertical'` replaced with `resize: 'none', overflow: 'hidden'`. `useCallback` was already imported — no new imports required. No backend changes. |
| v1.0.30 | 2026-05-11 | Backend-only: fix AI calling test-blind schools "test-optional" (app#115) — strengthened `test_policy=free` rule in `skill-core.md`; added all CSU campuses by popular name; DB fix (8 NULL rows → free). See backend CLAUDE.md v1.0.30. No frontend changes. |
| v1.0.29 | 2026-05-11 | Backend-only: fix `POST /research-sessions/{id}/generate-summary` returning 403 for counselors and tenant admins on student-owned sessions — see backend CLAUDE.md v1.0.29. No frontend changes. |
| v1.0.28 | 2026-05-11 | Backend-only: sweep cron for orphaned research sessions with no summary (app#105) — see backend CLAUDE.md v1.0.28. No frontend changes. **Action required:** create `sweep-session-summaries-prod` cron in Render dashboard. |
| v1.0.27 | 2026-05-11 | Backend-only: fix `GET /reports/unified` missing student self-conducted research sessions (app#105) — see backend CLAUDE.md v1.0.27. No frontend changes. |
| v1.0.26 | 2026-05-09 | Backend-only: consolidate student access control + fix tenant admin gaps (next#46) — see backend CLAUDE.md v1.0.26. No frontend changes. Filed next#47 (friendly 403 error state when navigating to inaccessible session — not yet implemented). |
| v1.0.25 | 2026-05-08 | Invite email overhaul + Add Family modal engagement fields (app#114, frontend portion). **`src/components/counselor/AddFamilyModal.tsx`:** new state for `engagementType`, `packageName`, `startDate`, `expectedEndDate`, `actualEndDate`, `editateEnabled`, `editateReviewLimit`, `graduationYear`; new "Engagement Details (optional)" section in form with engagement type dropdown, package name text input, start/expected/actual end date pickers (3-column row), and a second row with grad year, feedback rounds, and Editate enabled checkbox; all fields included in request body to both `/counselors/me/families` and `/tenant-admin/families`; all fields cleared on reset. **`src/app/dashboard/page.tsx`:** fixed focus-loss-on-keystroke bug in `EditPanel` — `Field`, `Select`, and `DateInput` were defined as arrow functions inside the component, causing React to see new component types on every state update and remount their children (dropping input focus after each character typed); moved all three outside `EditPanel` as named functions at module scope; `Select` renamed `PanelSelect` to avoid collision with HTML `select`; `form` and `set` passed as explicit props. Backend changes in backend CLAUDE.md v1.0.25. |
| v1.0.23 | 2026-05-05 | Backend-only: CC sending coach on their own session report emails (app#112) — see backend CLAUDE.md v1.0.23. No frontend changes. |
| v1.0.22 | 2026-05-04 | Product tour — role-specific onboarding walkthrough (next#44, frontend-only). **New files:** `src/lib/tourContent.ts` — single source of truth for all tour copy (step titles, body, bullet lists, showcase items) for all three roles (student, counselor, parent); `src/components/tour/ProductTour.tsx` — react-joyride v3 wrapper (dynamic-import, `ssr: false`); `src/components/tour/TourShowcaseModal.tsx` — post-tour showcase modal listing off-screen features. **Modified files:** `src/components/chat/ChatHeader.tsx` — replaced bare `?` button with `HelpMenu` component; `src/components/chat/ChatInterface.tsx` — added tour auto-trigger after `fetchUsage`, `ProductTour` always-mount when signed in, tour IDs on key UI elements. **Tour mechanics:** auto-triggers on first `/chat` visit per role; role-specific key written to localStorage (`soar_tour_seen_student/counselor/parent`) on Skip or Finish — never fires again; re-triggerable from ? menu → "Take a tour". **HelpMenu (`src/components/chat/HelpMenu.tsx`):** dropdown replacing bare ? button; three items: Take a tour (signed-in only), How are sessions counted?, FAQ link. **Visual design:** tooltip header is dark navy (`#0c1b33`) with sky blue title text (`#7dd3fc`); arrow matches header; white body/footer with hairline separator; indigo-600 primary button with shadow; showcase modal matches with dark navy header band and chevron affordances per feature row. **Tour content:** student (7 steps starting on chat input with "Welcome to Soar"), counselor (9 steps with centered welcome card first), parent (7 steps with centered welcome card first); each ends with a TourShowcaseModal listing off-screen features (Profile, Personalized College Fit Summaries, Likelihood Assessment, Scholarship List, Enrichment Programs for students; Meeting Brief, AI Session Reports, Likelihood, Soar Summary, Dashboard for counselors; Likelihood, Personalized College Fit Summaries, Net Price Calculators, Session Reports for parents). **react-joyride v3 API notes:** named export `{ Joyride }` (not default); callback prop is `onEvent` (not `callback`); type is `EventData`; `skipBeacon: true` (not `disableBeacon`); styles use top-level `options` prop (not `styles.options`); primary button key is `buttonPrimary` (not `buttonNext`); `showProgress`/`buttons`/`overlayClickAction` in `options`. No backend changes. |
| v1.0.21 | 2026-05-03 | Fix multi-tab sign-in UX on landing page (next#43, frontend-only): `src/app/page.tsx` — two changes. (1) Added `useEffect` client-side redirect: when `isLoaded && isSignedIn`, calls `router.replace('/chat')` immediately — serves as a fallback when the server-side `proxy.ts` redirect doesn't fire (e.g. Brave Shields blocking the session cookie in certain tab configurations). (2) Replaced both `<SignInButton mode="modal">` instances with `<Link href="/sign-in">`: the nav "Sign in" button and the "Already have access? Sign in" link at the bottom of the request-access form now navigate to the full `/sign-in` page instead of opening a Clerk modal overlay. Full-page navigation lets Clerk re-initialize fresh and detect any existing session from localStorage, then auto-redirect to `/chat` — the modal path skipped this re-initialization. Removed now-unused `SignInButton` from the `@clerk/nextjs` import (replaced entirely by `useAuth`). **Root cause analysis:** When a user has Soar open in Tab 1 (signed in) and the landing page in Tab 2 and clicks Sign In, Brave with Aggressive fingerprinting protection isolates localStorage per-tab, so Clerk sees no session in Tab 2 and prompts for credentials. This is expected Brave privacy behavior — not a Soar bug. The two fixes improve UX for Chrome/Firefox/Safari and non-strict Brave users where cross-tab session sharing works normally; in strict Brave isolation, re-authentication per tab is unavoidable. **Security verified:** Clerk correctly handles the "already signed in" case on the `/sign-in` page — if a session is active, it redirects to `/chat` as the authenticated user rather than allowing sign-in as a different user without signing out first. Backend JWT validation is per-request and independent of frontend session state; no cross-user data contamination is possible. No backend changes. |
| v1.0.20 | 2026-05-02 | Backend-only: remove fuzzy DB lookups for manually-added enrichment programs and colleges (app#111) — see backend CLAUDE.md v1.0.20. No frontend changes. |
| v1.0.19 | 2026-05-02 | Fix session report date defaulting to next day for US West Coast users after ~5 PM PDT (next#41, frontend-only): `reports/page.tsx` — two occurrences of `new Date().toISOString().slice(0, 10)` replaced with `localDateStr(new Date().toISOString())!`. `toISOString()` always returns UTC, which rolls over to the next calendar day for PDT (UTC-7) users after ~5 PM. `localDateStr()` (already in the file, used for research summary and note dates since v0.9.24) uses `getFullYear()/getMonth()/getDate()` — local time — so the correct local date is used. Two sites fixed: (1) `saveBriefToReport()`: meeting brief "Save as Report" was POSTing tomorrow's date as `appointment_date`, so the saved report appeared with the wrong date unless the counselor noticed and manually corrected it. (2) `fmtTime()` fallback: used when rendering a time + timezone label with no explicit date context. No backend changes. |
| v1.0.18 | 2026-05-02 | Fix stale session counts + session reset cron (app#110, frontend): `SessionsHelpModal.tsx` — updated reset timing copy from "Limits reset on the 1st of each month" to "Limits reset at 8 AM UTC (Greenwich Mean Time) on the 1st of each month" to match the actual Render cron schedule. Backend changes (Part 1: `/my-usage` read-time fix; Part 2: `/internal/session-reset` endpoint + cron) documented in backend CLAUDE.md v1.0.18. |
| v1.0.17 | 2026-05-02 | Fix multi-child parent landing on blank student state (next#42, frontend-only): `ChatInterface.tsx` — two changes. (1) Parent auto-select effect: changed condition from `myStudents.length === 1` to `myStudents.length >= 1` — now auto-selects the first student for all parents (not just single-child) when no valid selection exists. Removed the `else` branch that was explicitly clearing `forStudentId` to null for multi-child parents, which was the root cause of the blank state. (2) Blank dropdown option: changed render condition from `(!isParent || myStudents.length > 1)` to `!isParent` — parents never see the blank "— Select student —" option since they have no meaningful "no student" state. No backend changes. |
| v1.0.16 | 2026-04-30 | Backend script-only: rewrite `gen_family_invites.py` as argument-driven CLI. No frontend changes. See backend CLAUDE.md v1.0.16. |
| v1.0.15 | 2026-04-30 | Fix Meeting Brief heading showing wrong student name during generation (frontend-only): `generateBrief` in `reports/page.tsx` cleared `briefText` but not `briefStudentName` at the start of a new request, so the heading displayed the previous brief's student name for the entire duration of generation. Fix: look up the student name from the local `students` array using the passed `studentId` and call `setBriefStudentName` immediately before the fetch — heading is now correct from the first frame. Added `students` to `useCallback` deps. No backend changes. |
| v1.0.14 | 2026-04-29 | Backend-only: fix tenant admin blocked from student lists, profiles, and activities (app#113). No frontend changes. See backend CLAUDE.md v1.0.14. |
| v1.0.13 | 2026-04-29 | Fix wrong Peterson's data on manually-added scholarships + deadline override ignored (frontend): (1) `ScholarshipEditDrawer` auto-refresh guard in `lists/page.tsx`: added `entry.in_db &&` to `needsRefresh` — auto-refresh from DB only fires for entries added via Peterson's search (`in_db=true`); manual entries (`in_db=false`) are skipped. This was the proximate cause: even after the add endpoint was fixed, the drawer was calling `refresh-from-db` automatically on open, re-attaching wrong data from an unrelated scholarship. (2) `formatScholarshipDeadline` in `lists/page.tsx`: fixed evaluation order from `deadline_date \|\| custom_deadline` → `custom_deadline \|\| deadline_date` so the Deadline Override the counselor enters takes precedence over the Peterson's DB date rather than being silently ignored. Backend changes (add endpoint + refresh-from-db endpoint) documented in backend CLAUDE.md v1.0.13. |
| v1.0.12 | 2026-04-29 | Fix student switch not clearing sessions sidebar + stale session data (next#40, frontend-only): `ChatInterface.tsx` — four coordinated fixes. (1) **Student-switch reset effect**: added `prevForStudentIdRef = useRef<number \| null \| undefined>(undefined)` sentinel (`undefined` = initial mount, skip; `null`/number = genuine value). On student change: aborts any in-flight chat stream, clears all per-student chat state (messages, conversationHistory, serverSessionId, activeSessionId, currentResearchSessionId, input, isStreaming, streamingMessageId), calls `setSessions([])` + `setIsInitializing(true)` immediately — old student's sessions are never visible under the new student even for a fraction of a second; skeleton appears while new sessions load. Effect deps: `[forStudentId]` only (no fetchSessions — separation of concerns). (2) **Sessions effect**: added `forStudentId` directly to deps `[isLoaded, userId, forStudentId, fetchSessions]` so it fires on every student change without relying on the indirect `forStudentId → fetchSessions recreated → effect fires` chain that was silently failing in practice. `fetchSessions` `finally` block already calls `setIsInitializing(false)`, dismissing the skeleton when the fetch completes. (3) **fetchSessions branch 1**: removed `(isCounselor \|\| isParent)` gate — now uses `forStudentId` unconditionally when set. On page reload, `fetchSessions` fires before `fetchUsage` completes, so `isCounselor` is still `false`; the old condition fell through to `unscoped=1` and returned the counselor's own sessions. Backend enforces access control regardless, so the role-flag gate is unnecessary. (4) **Stale localStorage guard in fetchUsage**: when `account_type` is student (not counselor, not parent), immediately calls `setForStudentId(null)` + `localStorage.removeItem('ll_for_student_id')` — prevents a student from inheriting a counselor's `ll_for_student_id` when the counselor closed the tab without signing out and the student then opens the same browser. (5) **Cross-tab sync**: `window.addEventListener('storage', ...)` fires when `ll_for_student_id` changes in another tab, syncing `forStudentId` state so switching students in tab B is immediately reflected in tab A. No backend changes. |
| v1.0.11 | 2026-04-28 | Tenant admin practice-wide dashboard visibility (app#106, frontend): `dashboard/page.tsx` — `DashboardStudent` interface gains `assigned_counselor?: string | null` and `actual_counselor_id?: number | null` fields (populated by backend for tenant admin queries). Edit drawer Overview section shows read-only "Assigned Coach: [name]" label when `assigned_counselor` is present. Mobile card list and desktop table both show "Coach: [name]" in blue under the student email when present. No new API calls; all data comes from the existing `GET /counselor/dashboard/students` response. Backend changes (5 endpoints) documented in backend CLAUDE.md v1.0.11. |
| v1.0.10 | 2026-04-28 | Fix student seeing wrong student's profile in chat (next#39, privacy): `ChatInterface.tsx` chat POST body included `for_student_id` whenever `forStudentId` was truthy — with no check that the sender is a counselor or parent. A stale `ll_for_student_id` value in localStorage from a prior counselor session could persist into a student account, causing the backend to load the wrong student's profile into the system prompt. Fixed by adding `(isCounselor \|\| isParent)` guard to the `for_student_id` field in the chat POST body, matching the existing guard in `fetchSessions`. Backend test policy fix (app#112) documented in backend CLAUDE.md v1.0.10. |
| v1.0.8 | 2026-04-27 | Fix per-student scheduling link save/clear not working (app#109, app#110): (1) `dashboard/page.tsx` `EditPanel.save()`: changed `onSave({...student, ...data})` → `onSave({...student, ...form})` — backend returns `{ok:true}`, not the updated row, so the old code spread nothing useful and the parent list retained stale values; now merges the current form state so the drawer reflects edits immediately without a page refresh. (2) `admin/page.tsx`: added `consultation_link`, `scheduling_link_comprehensive`, `scheduling_link_hourly` to `UserRow` interface, `saveUser` PATCH body, and admin edit modal — new "Scheduling Links" section appears when editing a counselor, with three URL inputs (consultation, comprehensive, hourly) matching the resolution order in `GET /my-usage`. |
| v1.0.7 | 2026-04-27 | Fix chat sidebar loading flash (next#38): `isInitializing` state added to `ChatInterface.tsx` — starts `true` on mount, resets to `true` in the user-switch effect, and is set to `false` in `fetchSessions`' `finally` block. While `isInitializing` is true and the user is signed in, the session list renders three animated skeleton bars (`animate-pulse`, `bg-white/10`) instead of "No past conversations yet." — eliminating the 1-2s empty-state flash that appeared while auth + data loading completed. Works for all account types (student, counselor, parent). For students, the skeleton clears after the first `fetchSessions` call (`?unscoped=1`), then sessions update again once `fetchUsage` sets `myDbUserId` and the correct `?for_student_id=` call fires. |
| v1.0.6 | 2026-04-27 | Fix Soar Summary not persisting after drawer close for students (app#108): Two-part frontend-only fix in `src/app/lists/page.tsx`. (1) `onUpdateEntry` no longer fires a redundant PATCH — the streaming endpoint already committed the summary to DB before the response body closes, so a second PATCH was causing a stale-read race: the PATCH response could return the pre-existing `soar_research_summary` (e.g., a counselor's old summary) and overwrite the optimistic update, making the new summary vanish on drawer reopen. Now `onUpdateEntry` only updates `entries` and `editEntry` local state. (2) `handleGenerateSummary` now calls `setForm()` with the generated summary text, so if the user subsequently clicks Save, the Save PATCH body includes `soar_research_summary` — persisting it to DB idempotently alongside the other form fields. `onUpdateEntry` prop type changed from `Promise<void>` to `void` (now synchronous). |
| v1.0.5 | 2026-04-26 | Two `ChatInterface.tsx` bug fixes (next#37): (1) 403 infinite loop on account switch — stale `ll_for_student_id` localStorage key from a counselor session persisted into the next login; state-clearing effect now calls `setForStudentId(null)` + `localStorage.removeItem('ll_for_student_id')` on user switch. Also added `myDbUserIdRef` to track the authenticated user's DB integer ID via ref, cleared on switch. (2) Student shared-session visibility race condition — `fetchUsage()` and `fetchSessions()` were called simultaneously in a combined `useEffect`; `fetchSessions` ran before `fetchUsage` had set the user's DB ID, so students fell through to `?unscoped=1` and only saw their own sessions. Fix: added `myDbUserId` state (a number, safe in deps — no loop risk); `fetchSessions` depends on it; split combined effect into two separate effects so `fetchSessions` only fires after `fetchUsage` commits the user ID and role flags to state. `forStudentId` path in `fetchSessions` now also guarded by `isCounselor \|\| isParent` to prevent student accounts from ever hitting it via stale localStorage. |
| v1.0.3 | 2026-04-25 | Shared session visibility + auth timing race fixes (next#36, app#98): (1) `fetchSessions` in `ChatInterface.tsx` now calls `?for_student_id=their_db_id` for students instead of `?unscoped=1`, so students see all sessions conducted for them by counselors/parents. `isCounselor`, `isParent`, and `usageData` added to `fetchSessions` deps so it re-runs after `fetchUsage` populates the user's DB ID. (2) Auth timing race on account switch: `isLoaded` from `useAuth()` now guards all four auth-dependent `useEffect` hooks — no API calls fire while Clerk is mid-session-switch. `prevUserIdRef` + state-clearing effect wipes all auth-scoped state (`usageData`, `sessions`, `messages`, `myStudents`, role flags, etc.) when `userId` changes, preventing previous user's data from bleeding into the new session. |
| v1.0.2 | 2026-04-24 | Fix add-to-list buttons missing in counselor mode: `extractResearchListOffers`, `extractScholarshipListOffers`, and `extractEnrichmentListOffers` in `ChatMessage.tsx` now match `her\|his\|their` in addition to `your` and `[Name]'s`. Fix silent auth/sync failure on parent/counselor onboarding: `handleSaveData()` in `onboarding/page.tsx` now checks `syncRes.ok` for all account types. |
| v0.9.6.2 | 2026-04-10 | Unified reports page (#28); sidebar scoping (#49); summary modal spinner |
| v0.9.6.3 | 2026-04-10 | Family onboarding (#38, #39): AddFamilyModal, Invited badges with copy-link on dashboard and admin; WelcomeCard first-session starters (#48 partial) |
| v0.9.6.5 | 2026-04-11 | Extended onboarding suite (#48): 6-step flow (role picker → profile → colleges → Soar intro cards → counselor invite → question picker); auto-send first question to chat via sessionStorage; role-specific card content and taglines; parent onboarding flow; migration invite routing; strikethrough rendering fix; add-to-list buttons hidden for parents |
| v0.9.6.7 | 2026-04-14 | Backend-only: complete data migration (#61, #60) — no frontend changes |
| v0.9.7 | 2026-04-15 | Backend-only: production deployment infrastructure + Clerk allowlist (#69) — schema fixes, tiers seeding, invite-only allowlist gate — no frontend changes |
| v0.9.7.1 | 2026-04-15 | Backend-only: scholarship search overhaul (#54, #24) — coaching intake approach, eligibility scoring, filter fixes, merit-first framing, strategy guide auto-load — no frontend changes |
| v0.9.7.2 | 2026-04-16 | Fix research summaries not showing on Reports page (#70): loadReports() in reports/page.tsx was bailing early with setReports([]) when studentId was null instead of calling /reports/unified; fixed to always call the API and pass for_student_id only when a student is selected |
| v0.9.7.3 | 2026-04-16 | Production config only (#71): Essays module enabled, Editate credentials set on production tenant, essay lazy-fetch confirmed working — no frontend code changes |
| v0.9.7.4 | 2026-04-16 | Beta banner + Render cleanup (#36, #22, #23): BetaBanner component added (dismissible, signed-in users only, 7-day reappear); Render now API-only with 302 redirects to SOAR_BASE_URL — no longer serves static/index.html |
| v0.9.7.5 | 2026-04-16 | WelcomeCard copy refresh (#75): 6 value prop cards (up from 5), updated starter chips (removed "safety/match/reach" terminology), tagline kept as original personalization-focused line |
| v0.9.7.6 | 2026-04-17 | Redirect lifelaunchr-next.vercel.app → soar.lifelaunchr.com (#72): host-based redirect in next.config.ts; preview deployment URLs excluded |
| v0.9.7.7 | 2026-04-17 | Landing page + upgrade page rewrite (#32): new hero copy on landing page; full upgrade page with fear-first framing, feature cards, tier tables, tools comparison, FAQ; layout.tsx metadata updated; tenant tagline updated to "College and Career Planning, Built for the Whole Team." |
| v0.9.7.8 | 2026-04-17 | Server-side auth redirect (#77): signed-in users hitting / now redirect to /chat at the edge via proxy.ts (Next.js 16 middleware); deleted conflicting middleware.ts; removed client-side Clerk loading guard from page.tsx to eliminate blank-page flash for non-signed-in users |
| v0.9.8 | 2026-04-17 | Tenant admin family invite (#80): AddFamilyModal shows counselor dropdown for tenant admins and routes to POST /tenant-admin/families; dashboard/page.tsx fetches counselor list and passes to modal with key-prop remount fix; localStorage token fix (was sessionStorage, lost across Clerk redirects); site password gate query-string fix in proxy.ts (url.pathname + url.search — was dropping ?token= param); parent onboarding routing fix (POST /accept-invite response used as primary account_type source so parents skip role picker and land on Soar intro at step 4) |
| v0.9.9 | 2026-04-18 | Essays empty state contextual messages (#83): three distinct states now shown — Editate not enabled for student (admin instructions), Editate not enabled for own account (contact counselor), Editate enabled but no colleges in Applying+ status (link to Lists page). essays/page.tsx uses effectiveEditateAvailable to branch the message correctly. |
| v0.9.10 | 2026-04-19 | Backend-only: agentic loop graceful degradation (#45) — friendly error messages on API failures, synthesis nudge + tool_choice=none to guarantee loop termination. No frontend changes. |
| v0.9.10 | 2026-04-20 | Backend-only: quarterly data refresh wizard (#89) — scripts/quarterly_refresh.py with --dry-run mode; build_colleges_csv.py auto-detects newest Peterson's folder by mtime. No frontend changes. |
| v0.9.10 | 2026-04-20 | Invite approval banner (#66): admin/page.tsx shows amber banner when approve returns ok:false (active account), green on success. approveMessage state added; all error paths covered including network failures. Follow-up for inline per-row errors filed as next#16. |
| v0.9.10 | 2026-04-20 | Onboarding + invite form copy (#17): practice name label on landing page changed to "Company, practice, or school name"; helper text added to onboarding IEC practice field explaining it is the business name with examples. |
| v0.9.11 | 2026-04-20 | Sidebar nav scroll fix (#18): bottom nav div given overflow-y-auto + max-h-[60vh] so Activities and other items are always reachable on small screens; counselors with a student selected no longer clip the nav. |
| v0.9.11 | 2026-04-20 | Add-to-list button fixes (#87): ChatMessage.tsx extract functions switched from match() to matchAll()+g flag — multiple colleges in one response now each get their own button. Strip ** before matching so bolded college names produce buttons. Require [A-Z] first char to reject generic phrases like "any of these". Filed next#19 for future hidden-marker redesign. |
| v0.9.11 | 2026-04-20 | Resend invite button for access requests (#13): admin/page.tsx shows "Resend invite" button on invited rows alongside the invited date. Calls the same approveRequest() function; backend returns status="resent" so banner shows "Invite resent successfully." instead of "Invite sent successfully." |
| v0.9.12 | 2026-04-21 | Session counter fix on profile page (#51): profile/page.tsx now shows sessions_used / session_limit from /my-usage instead of the legacy messages_used / effective_limit fields. Students see the correct pool total (e.g. 35) rather than their own tier limit (e.g. 5). |
| v0.9.12 | 2026-04-21 | Parent profile page cleanup: Academic Information, Background, High School, College Interests, and Preferences & Goals sections now hidden for parents viewing their own profile (gate: accountType !== 'parent' \|\| isViewingStudent). Added a "Financial Preferences" section (income tier, SAI/EFC, budget max) with explanatory note. Removed History retention row from Plan & Usage table — field is not enforced in the backend. Filed next#21 to wire financial prefs into system prompt (post-launch). Stale localStorage forStudentId bug (counselors with no students seeing student nav items) filed as next#20 (post-launch). |
| v0.9.13 | 2026-04-22 | College drawer tab visibility: tab labels shortened ("Research Notes"→"Research", "People Notes"→"People") and horizontal padding reduced (container 22px→10px, buttons 14px→10px per side) so all 6 tabs fit in the 520px drawer without horizontal scrolling. "Soar Summary" was previously hidden off-screen unless the user scrolled right. (`src/app/lists/page.tsx`) |
| v0.9.13 | 2026-04-22 | Sidebar nav discoverability: "My Lists" moved to appear immediately after "Profile" for students — was previously after Research Summaries and Activities, hidden below the fold on typical screens. For counselors/parents viewing a student, "Lists" now appears before "Activities". "My Activities" for students is now between "My Lists" and "Research Summaries". (`src/components/chat/ChatInterface.tsx`) |
| v0.9.17 | 2026-04-22 | Monitoring stack (app#82): `@vercel/analytics` installed, `<Analytics />` added to `src/app/layout.tsx`. UptimeRobot monitors live for frontend + backend. Backend `/health` now accepts HEAD + GET. Sentry deferred post-launch. |
| v0.9.16 | 2026-04-22 | Suppress password manager autofill on CC email field (next#27): "Additional email addresses" input in session reports was triggering browser/password-manager credential autofill due to email-like placeholder. Added `autoComplete="off"`, `data-form-type="other"` (Dashlane), `data-lpignore="true"` (LastPass), `data-1p-ignore="true"` (1Password). No backend changes. (`src/app/reports/page.tsx`) |
| v0.9.15 | 2026-04-22 | Fix NPC link opening as relative URL (next#23): `sc_npc_url` from the DB sometimes lacks an `https://` prefix. The `href` on the "(Link ↗)" anchor in the Financial tab now guards with `.startsWith('http') ? url : \`https://${url}\``. (`src/app/lists/page.tsx` — `EditDrawer` Financial tab) |
| v0.9.14 | 2026-04-22 | Fix enrichment AddEnrichmentModal showing 'Add "undefined"' (next#29): backend `/enrichment-lookup` was aliasing `name AS program_name` but `EnrichmentSearchResult` interface expected `name` — alias removed in backend. Frontend unchanged. (`lifelaunchr-app-3/main.py`) |
| v0.9.14 | 2026-04-22 | Fix enrichment tab count showing 0 until clicked (next#29): `loadEnrichment()` was lazy (tab-click only). Now fired in parallel with the initial `GET /lists/{id}` fetch at page load via `Promise.all`. `enrichmentLoaded` flag set immediately so lazy guard prevents double-fetch. (`src/app/lists/page.tsx`) |
| v0.9.13 | 2026-04-22 | Fix: coach assessment fields (EC rating, Academic Rigor) reset to -Select after save (#25). Root cause: GET /profile returns DB column names (`extracurricular_rating`, `academic_rigor`) but the selects bind to `ec_rating` and `academic_rigor_rating`. Post-save re-fetch was wiping the select values. Added `normalizeProfile()` in `src/app/profile/page.tsx` to map DB names → frontend names on both initial load and post-save re-fetch. |
| v0.9.25 | 2026-04-23 | Backend-only: counselor families endpoint (#90) — `_find_or_create_user()` now handles all 4 cases (created/reinvited/reactivated/connected); connected notification email Sign In link now uses `_SOAR_BASE_URL`; `send_connected_notification_email()` gains `app_url` param. No frontend code changes. |
| v0.9.24 | 2026-04-23 | Fix note and research summary dates showing tomorrow for late-evening US users (next#35): `reportLabel()` was using `created_at?.slice(0,10)` and `started_at?.slice(0,10)` to get the display date — slicing a UTC ISO timestamp gives the UTC date, which is one day ahead for US users after ~5 PM local time. Added `localDateStr()` helper in `src/app/reports/page.tsx` that converts a UTC timestamp to a local YYYY-MM-DD string using `getFullYear()/getMonth()/getDate()` (local clock). Applied to note `created_at` label and all three research summary `started_at` display sites. |
| v0.9.23 | 2026-04-23 | Fix profile re-fetch wipes unsaved edits (next#26): `useEffect` in `src/app/profile/page.tsx` had `clerkUser` (the full object) as a dependency. Clerk silently creates a new object reference on every background token refresh (~60s), causing the effect to re-run, re-fetch the profile from the server, and overwrite any pending unsaved changes — explaining why citizenship, testing_status, and other fields "required 2-3 saves." Fixed by changing the dependency to `clerkUser?.id`. One-line change; no backend changes. |
| v1.0.2 | 2026-04-24 | Fix add-to-list buttons missing in counselor mode: `extractResearchListOffers`, `extractScholarshipListOffers`, and `extractEnrichmentListOffers` in `src/components/chat/ChatMessage.tsx` now match `her|his|their` in addition to `your` and `[Name]'s`. Counselors researching on behalf of a student were seeing "Want to add X to her research list?" with no button because the regex only matched "your". |
| v1.0.1 | 2026-04-24 | Fix silent auth/sync failure on parent/counselor onboarding: `handleSaveData()` in `src/app/onboarding/page.tsx` now checks `syncRes.ok` for all account types before proceeding — previously only students had the guard, so a failed `/auth/sync` response silently advanced parents/counselors to step 4 with no user row claimed. Added `console.error('[onboarding] handleSaveData error:', err)` to the catch block so failures are visible in browser DevTools without needing the network tab. |
| v1.0 | 2026-04-23 | Production go-live. No frontend code changes — all v1.0 work was backend migration quality fixes (html2text paragraph collapse, EC/athletic rating key mapping, UC/CA activity categories, org_description, Key to-dos paragraph break in session notes) and data backfill (sent_at on 10,570 session reports). Backend: see lifelaunchr-app-3 CLAUDE.md v0.9.26–v0.9.28. |
| v0.9.22 | 2026-04-23 | Fix onboarding skip for newly invited users (#93 follow-up): `NEXT_PUBLIC_CLERK_AFTER_SIGN_UP_URL=/chat` in Vercel env vars was overriding `fallbackRedirectUrl="/onboarding"` on the `<SignUp>` component — all new sign-ups landed directly in chat, bypassing profile setup, college selection, and the Soar intro cards. Removed from staging and production Vercel env vars. Updated `.env.local.example` to document the correct value (`/onboarding`). No code changes — the `<SignUp fallbackRedirectUrl="/onboarding" />` setting in `src/app/sign-up/[[...sign-up]]/page.tsx` was always correct. |
| v0.9.18 | 2026-04-22 | Soar Summary fixes (#91): (1) `summaryError` state + red error banner in the Soar Summary tab — errors are now visible instead of silent. (2) `onUpdateEntry` prop added to `EditDrawer` — updates parent `entries` array and `editEntry` state without closing the drawer; used after generation completes so the summary persists when the drawer is closed and reopened. (3) Fixed drawer closing on "Regenerate Summary" — was caused by `handleGenerateSummary` calling `onSave` (which calls `setEditEntry(null)`); switched to `onUpdateEntry` which has no close side-effect. All in `src/app/lists/page.tsx`. |

## Repository Structure
- **Backend:** `lifelaunchr-app-3/` — Python/FastAPI, deployed on Render
- **Frontend:** `lifelaunchr-next/` (this repo) — Next.js, deployed on Vercel

## Branch → Environment Mapping

| Branch | Backend (Render) | Frontend (Vercel) | Notes |
|---|---|---|---|
| `main` | Staging (`lifelaunchr.onrender.com`) | Preview/staging | Active development |
| `production` | Production (`lifelaunchr-prod.onrender.com`) | Production (`soar.lifelaunchr.com`) | Stable live code |

**Practical rule: push to `main` for staging, test there first, then push to `production` only when ready. Never tag a version until after it has been pushed to production and verified.**

> ⛔ **NEVER push to `production` without the user explicitly saying "push to production" or "looks good on staging." No exceptions, even for trivial or low-risk fixes. Always push to `main` first, tell the user to test on staging, and wait for explicit confirmation before touching `production`.**

> 🚫 **Claude must NEVER run the production push command.** After completing Step 3, write out the exact command(s) the user should run, then stop. The user copies and runs them after testing on staging. This rule exists because Claude repeatedly rationalizes around the confirmation gate — making it physical (user must type the command) is the only reliable fix. No exceptions for "small fixes," "wiring check passed," or "it was already on staging."

### Step-by-step workflow

**Step 1 — Develop and push to `main` (staging)**
- All code changes are committed and pushed to `main`.
- Vercel deploys automatically from `main` as a preview/staging build.
- Test on staging. Confirm the fix works before touching production.

**Step 2 — Decide what to push to production**
- May be all of `main` (fast-forward) or specific commits (cherry-pick).
- Confirm no Claude worktree has stranded commits that should land in `main` first.
- **Do not delete the current Claude worktree** — deleting it ends the active chat session. Only close a worktree when we explicitly decide to wrap up that session.

**Step 3 — Pre-production checklist**
1. Review what's changing — no accidental deletions:
   ```bash
   git diff origin/production..origin/main --stat
   git diff origin/production..origin/main
   ```
2. Update `CLAUDE.md` with all UI/component changes and commit to `main`.
3. For backend changes, also run `check_wiring.py` and update `FEATURES.md` — see the backend `CLAUDE.md` (`lifelaunchr-app-3`) for the full backend checklist.

**Step 4 — Push to production (USER RUNS THIS, NOT CLAUDE)**

Claude writes out the command(s) below and stops. The user runs them after confirming staging looks good.

To push all of `main` to production (fast-forward):
```bash
git push --force-with-lease origin main:production
```

To push only specific commits (cherry-pick):
```bash
git checkout production
git cherry-pick <hash1> <hash2>   # in chronological order
git push origin production
git checkout main
```

**Never use** `git checkout production && git merge origin/main` — creates unnecessary merge commits and causes branch divergence. Always use the `main:production` form or cherry-pick.

**Step 5 — Verify the push**
```bash
git log --oneline -1 origin/main && git log --oneline -1 origin/production
```
Confirm production is at the expected commit. If something looks wrong, stop and investigate before tagging.

**Step 6 — Tag the version**

Only tag after production is verified:
```bash
git tag vX.Y.Z
git push origin vX.Y.Z
```
Tag both repos at the same version number when both were updated in the release.

### Branch rules
- **All commits go to `main` only.** Never commit directly to `production`.
- `production` may lag behind `main` — that is normal and expected. `main` must never be behind `production`.
- See the backend `CLAUDE.md` (`lifelaunchr-app-3`) for the full pre-production checklist including `check_wiring.py`.

## Key URLs
| Environment | Frontend | Backend API |
|---|---|---|
| Staging/dev | Vercel Preview (main branch) | `https://lifelaunchr.onrender.com` |
| Production | `https://soar.lifelaunchr.com` | `https://lifelaunchr-prod.onrender.com` |

## Local Development
- `NEXT_PUBLIC_API_URL` is set to the Render staging URL in `.env.local`
- Local Next.js dev server: `http://localhost:3000`
- Local backend (if running): `http://localhost:8000`

## What NOT to do
- **Do not push directly to `production` branch** — that affects live production users
- **Do not rename branches** without following the full rename checklist (see `docs/migration.md`)
- **Do not change Vercel Production branch** from `main` until we're ready to cut over
- **Do not break working features** — test chat, dashboard, safety flags, admin, and auth after significant changes

---

## Product Positioning and Messaging

Soar is positioned as **the AI college and career planning assistant built specifically for the counselor-student-parent team**. This framing should be consistent across all pages, copy, and any new UI.

### Core insight (the "TiVo moment")
Students are already using ChatGPT and Claude for college and career planning — whether their counselor knows it or not. Soar is the version where the counselor is in the loop. Every student conversation builds a shared research record visible to counselors and parents.

### Key differentiators vs. general-purpose AI (Claude, ChatGPT)
1. **Persistent student profiles** — built automatically from conversation; survives across sessions
2. **Real college and scholarship data** — 1,800+ colleges, 6,700+ scholarships, 250+ enrichment programs; no hallucinated acceptance rates
3. **Counseling methodology baked in** — asks the questions students don't know to ask; guides the process step by step rather than just answering queries
4. **Shared research record** — counselor, student, and parent all see the same research; no one is out of the loop
5. **Privacy** — uses the Anthropic API; student conversations are not used to train AI models (unlike Claude.ai/ChatGPT direct)

### Positioning vs. other tools
- **Scoir / Naviance / Maia Learning / College Planner Pro** — complementary; those are CRM/workflow tools; Soar is where the research happens. Key insight: students actually use Soar because it's conversational, not form-based.
- **NerdApply** — complementary; NerdApply does application strategy analysis; Soar handles earlier-stage research and planning.
- **Claude / ChatGPT** — "in addition, not instead." General-purpose AI is fine for practice operations (marketing, comms). Soar is the right tool for the student-facing work.

### Tone and framing rules
- Lead with **counselor fears and opportunities**, not feature lists
- Use **"college and career"** throughout — Soar covers majors, careers, essays, résumés, scholarships, and financial aid, not just college selection
- Avoid "safety, match, and reach" — Soar uses "likely, target, and reach"
- Avoid excessive em-dashes; use periods and colons instead
- The product is **not a smarter Google** — it guides the process, not just answers questions

### Approved taglines (as of v0.9.7.7)
| Context | Tagline |
|---------|---------|
| Signed-in header (tenant tagline in DB) | College and Career Planning, Built for the Whole Team. |
| WelcomeCard (returning users) | The college and career planning assistant that knows you, remembers everything, and gets smarter the more you use it. |
| Landing page hero headline | The AI College and Career Counseling Assistant That Transforms Your Practice |
| Upgrade page hero headline | Your students are already using AI for college and career planning. Soar helps you guide them as they do. |
| layout.tsx metadata description | The AI college and career planning assistant that keeps counselors, students, and parents on the same page. Built on real data, deep counseling methodology, and a shared research record. |

---

## Frontend Pages and Components

### Pages

| Route | File | Purpose |
|-------|------|---------|
| `/` | `app/page.tsx` | Public landing page — auth-aware; signed-in users redirect to /chat; hero with new headline "The AI College and Career Counseling Assistant That Transforms Your Practice"; body copy positions Soar vs. general AI; italic credential line; privacy callout; "How it works" (3 steps); request access form |
| `/chat` | `app/chat/page.tsx` | Main AI chat interface; sidebar with sessions, student picker, schedule button |
| `/profile` | `app/profile/page.tsx` | Student profile (My Info for counselors); GPA, test scores, major, demographics, coach assessment |
| `/lists` | `app/lists/page.tsx` | College, scholarship, and enrichment lists; spreadsheet and card views; drag-to-reorder |
| `/activities` | `app/activities/page.tsx` | Student extracurricular activities; drag-to-reorder; UC/Common App format export via chat |
| `/dashboard` | `app/dashboard/page.tsx` | Counselor student roster; status fields, engagement type, notes, scheduling links |
| `/reports` | `app/reports/page.tsx` | Session reports; two-panel layout (list + form); AI draft; send via SendGrid |
| `/admin` | `app/admin/page.tsx` | Admin panel; tabs: Users, Tiers (super-admin), Tenants (super-admin), Tenant Settings |
| `/safety` | `app/safety/page.tsx` | Safety event review (admin/counselor only) |
| `/upgrade` | `app/upgrade/page.tsx` | Plans & Pricing page — fear-first framing for counselors; hero + case-for-Soar intro; 6 feature cards; student and counselor tier comparison tables; "Works with your tools" section (Scoir/Naviance/Maia/CPP, NerdApply, Claude/ChatGPT); FAQ (9 items); bottom CTA; beta pricing lock-in note |
| `/onboarding` | `app/onboarding/page.tsx` | 6-step post-signup onboarding: (1) role picker, (2) student profile, (3) college wishlist, (4) Make the Most of Soar intro cards, (5) counselor invite (counselors only), (6) question picker → auto-sends to /chat |
| `/join` | `app/join/page.tsx` | Invite acceptance landing page |
| `/accept-invite` | `app/accept-invite/page.tsx` | Post-Clerk-auth invite claim page |
| `/writing` | `app/writing/page.tsx` | Writing Hub — tabbed hub for self-discovery, writing practice, and essay sections. Role-specific views: student (assignments + response editor + personality assessment), counselor (roster + assign panel + observation generator), parent (read-only progress). |
| `/essays` | `app/essays/page.tsx` | Editate essay prompt browser and draft status tracker. Styled in dark Tailwind matching the Writing Hub (restyled v1.0.38). |
| `/sign-in` | `app/sign-in/` | Clerk sign-in |
| `/sign-up` | `app/sign-up/` | Clerk sign-up |

### Key Components

| Component | Path | Purpose |
|-----------|------|---------|
| `ChatInterface` | `app/chat/components/ChatInterface.tsx` | Main chat UI; SSE streaming; sidebar drawer; for_student_id mode; first-session detection and flag flip |
| `ChatMessage` | `app/chat/components/ChatMessage.tsx` | Renders a single message; detects "Want to add X?" patterns and renders inline Add buttons; renders file_ready download buttons |
| `WelcomeCard` | `components/chat/WelcomeCard.tsx` | Welcome screen with starter chips; switches between normal and first-session modes (`isFirstSession` prop); role-specific starters via `FIRST_SESSION_STARTERS`; returning-user view shows 6 value prop cards (`VALUE_PROPS`) and 4 starter chips (`STARTERS`); tagline: "The college and career planning assistant that knows you, remembers everything, and gets smarter the more you use it." |
| `AddFamilyModal` | `components/counselor/AddFamilyModal.tsx` | Modal for counselors to add student + parents as a family; validates emails, handles capacity errors, shows invite results |
| `ModuleChips` | `components/chat/ModuleChips.tsx` | Topic pills below chat input ("Load extra guidance for: Athletics, Religious life, ..."); toggles active_topics sent to backend |
| `WritingWelcomeModal` | `components/writing/WritingWelcomeModal.tsx` | Role-specific welcome modal shown once per role per browser (`localStorage` key per role). Three versions: student (process ownership, HECA framing), counselor (workflow + pacing), parent (privacy + read access). `useWritingWelcomeSeen(role)` hook for conditional rendering. |
| `WritingCoachView` | `components/writing/WritingCoachView.tsx` | Full-page counselor roster view when no student is selected on `/writing`. Lists students with assigned/submitted counts; clicking switches to that student and shows the assign panel. |
| `AssignPanel` | `components/writing/AssignPanel.tsx` | Counselor panel to assign exercises to a student. Fetches exercises via `GET /writing/library?section_key=`, renders exercise list; "Assign" button POSTs to `POST /writing/assignments` with optional note and due date. |

### Key Frontend Patterns

**Mobile Responsive Layout (v0.9)**
All pages are mobile-responsive. Strategy varies by page:
- Pages with data tables (admin, lists): use `hidden sm:table-cell` / `hidden md:table-cell` Tailwind classes to hide lower-priority columns on small screens. Inline styles are used alongside className for responsive visibility.
- Reports page: uses an `isMobile` JS state (via `useState(() => window.innerWidth < 768)` + resize listener) to switch between a mobile single-panel layout (list → tap → detail) and a desktop two-panel layout. Inline styles throughout (no Tailwind) because Tailwind JIT doesn't compile dynamically constructed class strings.
- Chat: sidebar starts closed on mobile; opens as a partial-width drawer (`w-[85vw] max-w-[280px]`).
- Other pages: use `sm:` and `md:` Tailwind prefixes on padding, width, and layout classes.

**`for_student_id` (Counselor Research Mode)**
When a counselor selects a student in the chat sidebar, their user ID is stored in `localStorage` as `for_student_id`. It is sent as a request header on `/chat` calls. The backend loads that student's data into the system prompt and attributes list additions to the student's account.

**SSE Streaming**
Chat responses stream from `POST /chat` as Server-Sent Events. Event types: `status` (tool progress text), `text_start`, `text` (streaming token), `done`, `error`, `file_ready` (download button), `limit_reached` (show message limit modal).

**Message Limit Modal**
When the backend emits a `limit_reached` SSE event, the chat UI shows a modal explaining the limit has been reached and how to get more messages.

**"Want to add X?" Pattern**
`ChatMessage.tsx` regex-matches specific phrases in Claude's response text to render inline Add buttons. Supported patterns (case-insensitive):
- `Want to add [Name] to your research list?`
- `Want to add [Name] to [Student Name]'s research list?`
- Same patterns for `scholarship list` and `enrichment list`

The regex uses `(?:your|.+?'s)` to match both "your" and any possessive name (e.g. "Katheryn's"). This is needed because in counselor research mode, Soar uses the student's name instead of "your."

The name capture group requires an uppercase first letter (`[A-Z]`) — this rejects generic phrases like "any of these" which start lowercase. The `i` (case-insensitive) flag is intentionally absent for this reason.

All three functions (`extractResearchListOffers`, `extractScholarshipListOffers`, `extractEnrichmentListOffers`) use `matchAll` with the `g` flag and return `string[]`, so a response discussing multiple items renders one button per item rather than just the first.

Do not change these phrases without also updating the regex in ChatMessage.tsx.

**Extended Onboarding Flow (v0.9.6.5)**
The onboarding page (`app/onboarding/page.tsx`) is a 6-step flow:
1. Role picker (student / counselor / parent)
2. Student profile (GPA, test scores, state, grad year)
3. College wishlist (search and add colleges)
4. "Make the Most of Soar" — role-specific intro cards (2×2 grid) with tagline
5. Invite a family (counselors only) — inline form to invite a student + optional parent
6. Question picker — role- and data-aware starter questions + free text input

Step 6 stores the selected question in `sessionStorage` as `onboarding_first_question`. If a counselor invited a student in step 5, the student's ID is stored as `onboarding_for_student_id`. On `/chat` mount, a useEffect reads these values, sets `forStudentId`, and auto-sends the question after an 800ms delay. Both keys are cleared after use to prevent re-firing.

The `SOAR_CARDS` constant and `getStarterQuestions()` function in the onboarding page contain all role-specific content. Migration invites (accepted via `/accept-invite`) route into the extended onboarding at step 4 (skipping role picker and profile since those were already set).

**Parent-Specific Behavior**
Parents skip step 5 (invite) and go directly from step 4 to step 6. Parents cannot add items to college/scholarship/enrichment lists — the `onAddToList`, `onAddToScholarshipList`, and `onAddToEnrichmentList` props are set to `undefined` when `isParent` is true. This prevents the inline "Add to list" buttons from appearing in chat.

**Tailwind JIT Warning**
Do not construct Tailwind class names dynamically (e.g. `text-${color}-500`). JIT only compiles classes that appear as complete strings in the source. For layout properties that must be set conditionally, use inline `style` props or pre-define both class variants explicitly.
